# 2026-07-12 — kit upgrade v1.12.1 → v1.13.0

> **Status:** `complete` — PR #164 (`claude/kit-upgrade-v1.13.0`), lands by
> merge commit on `quality` green.

- **📊 Model:** fable-5 · high · kit-upgrade

**What this session was about:** Fleet-wide substrate-kit v1.13.0 distribution
wave (rung: order — coordinator-directed upgrade, same lane as the v1.12.1
#155 / v1.12.0 #146 / v1.11.0 #129 upgrades). Payload: dead boot-pointer fix,
boot-wired generated `docs/SKILLS.md` skill index, playbook-grade skill bodies
with an advisory grounds checker.

## What was done

- Synced to origin/main `fd36644`; tree stamps confirmed v1.12.1 before
  starting (`bootstrap.py:92 KIT_VERSION`, `substrate.config.json`).
- Asset sha256 three-way verified before staging: `bootstrap.py` v1.13.0 =
  `982b2667b2158aa77639fd44bca72ab8e047fb3bdabbde580937dd1325730959`
  (735,972 bytes; == release.json `sha256` == kit `828e450:dist/bootstrap.py`).
- Canonical recipe: staged `bootstrap.py.new` + `release.json` in repo root →
  `python3 bootstrap.py.new upgrade` (engine self-cleaned both inputs). All
  three stamps now `1.13.0` (bootstrap.py KIT_VERSION, `.substrate/state.json`,
  `substrate.config.json`).
- **Carve-out scan — verbatim:** `- carve-out scan: ran — no kit-owned live
  workflow installed, nothing to scan.` Correct N/A form (websites' born-red
  hold rides the folded `quality` lane; no live substrate-gate by design).
- **Doc classes:** consumer-edited: 15 · diverged: 2 (`CONSTITUTION.md`,
  `docs/AGENT_ORIENTATION.md` — both the template and the doc moved) ·
  template-improved: 1 (`.claude/CLAUDE.md`). Manual merge done for the
  AGENT_ORIENTATION delta's SKILLS.md wiring (planted-doc-set line + the
  "Recurring action?" pointer — this also cleared the `[reachable] SKILLS.md
  orphan` strict finding). `.claude/CLAUDE.md` applied via the engine's own
  `upgrade --apply-docs` (engine-classified consumer-untouched, hash
  re-recorded) — the live boot set now routes to `docs/SKILLS.md`.
  CONSTITUTION.md's delta (same SKILLS.md pointer bullet) NOT hand-merged —
  preserved in `.substrate/upgrade-report.md` § Template deltas; cite by git
  pin once this PR merges (`git show <merge-sha>:.substrate/upgrade-report.md`),
  the next upgrade overwrites the live file.
- Previous upgrade PR #155's report checked (`git show
  ebcc42f:.substrate/upgrade-report.md`): no template deltas, nothing
  outstanding carried forward.
- Exactly ONE new backup banked: `.substrate/backup/bootstrap-1.12.1.py`
  (sha256 `1055ca2c…e4aa` — matches the v1.12.1 release digest #155 recorded);
  pre-existing banks untouched per git status.
- Exact-pin test bumped: `tests/test_born_red_session_gate.py`
  `kit_version == "1.13.0"` (+ chain line in the docstring). Repo-wide grep
  found no other functional `1.12.1` pins; `docs/current-state.md` kit lines
  bumped (v1.13.0, #164 appended to the stepwise chain).
- Verified: `python3 -m pytest tests/ -q` — 213 passed (incl. the bumped
  pin); `python3 bootstrap.py check --strict` — sole red on the born-red head
  was the designed session-card hold (`HOLD (by design)`), green expected on
  this flip commit.

⚑ Self-initiated: no — coordinator-directed distribution order (v1.13.0
wave). In-passing: current-state kit-line bump + the AGENT_ORIENTATION
SKILLS.md manual merge (required for a green strict gate, and it IS the
payload's boot-wiring on this repo) — kit-upgrade-adjacent, per
#113/#129/#146/#155 precedent.

## 💡 Session idea

**Make `bootstrap.py check` fail (or loudly warn) when `docs/SKILLS.md`
exists but no boot-path doc reaches it, with a fix-it hint naming the exact
line to add.** This session hit the generic `[reachable] SKILLS.md: orphan`
finding and had to derive the fix from the diverged-doc template delta by
hand; on repos with diverged orientation docs (2 of the planted set here),
every consumer will hit the same seam — the kit plants SKILLS.md but its
wiring lives in docs the kit may no longer own. A finding that says "add
`docs/SKILLS.md` to AGENT_ORIENTATION's planted-doc list" turns a manual
merge into a paste. Worth having because v1.13.0's headline is that the
skill index is boot-wired — on diverged repos it silently isn't, unless the
upgrade session notices. Fleet-level (kit engine), so routed to the
coordinator via this card + run report rather than `docs/ideas/backlog.md`
(deduped there: no prior entry).

## ⟲ Previous-session review

The v1.12.1 upgrade session (#155) did well: recording the banked dist's
sha256 on its card let this session verify the new `bootstrap-1.12.1.py`
bank byte-for-byte against an independent record — the chain-of-custody
habit is now two links long and paid off both times. What it could have done
better: its "no other functional pins" grep conclusion was correct but its
method wasn't recorded (which excludes were used), so this session re-derived
the exclusion set from scratch. Concrete workflow improvement: the card's
grep bullet should paste the actual command — applied above implicitly by
listing the exclusions (bootstrap.py / `.substrate/` / historical cards) and
in the run report verbatim.
