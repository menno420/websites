# 2026-07-19 — planning pass v2: next-cycle-2026-07-19 (6 slices)

> **Status:** `complete` — planning pass v2; produced
> `docs/plans/next-cycle-2026-07-19.md` (6 value-ranked slices + routed-out
> list) and refreshed the NEXT-2 baton. Born red; this card held the PR red
> until this deliberate flip to `complete` as the LAST step.

- **📊 Model:** opus-4.8 · high · idea/planning

**What this session is about:** A fresh-sync planning pass at HEAD `7dfdca2`
(#447). The previous cycle plan (`next-cycle-2026-07-18.md`) is fully executed
and orders 001–038 are all closed, so the seat needed a re-scan of where the
buildable frontier actually is. This session mines the 💡 lines on the recent
`.sessions/` cards (2026-07-18+), the `docs/current-state.md` gaps, the
kit advisories, and the product growth edges, verifies each candidate against
the tree (crossing out anything already shipped or owner-gated), and lands one
binding plan doc plus a refreshed baton. No code logic changes — this is a
docs/control pass.

Work-ladder rung: coordinator-assigned planning pass — produce the next-cycle
plan doc and refresh the NEXT-2 baton toward the top-value seat slice.

⚑ Self-initiated: no — coordinator-assigned planning slice.

## What was done

- `docs/plans/next-cycle-2026-07-19.md` (new) — the binding next-cycle plan:
  six executable slices ordered by value-per-effort (1 · route
  `submissions_store` onto the shared `_db._Conn` shim; 2 · NAV reachability
  GET guard for `app/`; 3 · askverify probes for the observable-but-unprobed
  asks; 4 · committed signal-registry data file; 5 · auto-discovering
  vendored-copy AST core guard; 6 · `/directory` `.gba` download probe follows
  redirects), a hygiene fold-in list, an explicit routed-out (owner/content/
  hub-venue-gated) section, and an honest sizing note that the seat-buildable
  frontier is thin this cycle.
- `control/status.md` — heartbeat refreshed: `phase` set to planning-pass-v2
  landed with queue top = `submissions_store`→`_db._Conn` shim, and the NEXT-2
  baton retargeted from the #447-era items to (1) route `submissions_store`
  onto `_db._Conn` (make the shim real) and (2) the `app/` NAV reachability GET
  guard. All prior neutral facts, pointers, the ARMED failsafe-cron token/id,
  the `acked/done=001-038` order ledger, and the health line preserved
  wholesale.
- `control/claims/plan-v2-2026-07-19.md` — work claim for this branch (deleted
  in this flip commit so it merges away clean, no drift-gate orphan).
- Verified (CI-equivalent, `DATABASE_URL` unset):
  `python3 -m pytest tests/ botsite/tests dashboard/tests review/tests -q`
  all-pass and `python3 bootstrap.py check --strict` green once this card flips
  `complete` (the born-red HOLD was the only gating finding, released at this
  flip).

**Verify plan:** four-suite
(`python3 -m pytest tests/ botsite/tests dashboard/tests review/tests -q`) +
`python3 bootstrap.py check --strict` before the flip push. This is a
docs/control PR — no code logic changed, so no runtime surface to drive.

## 💡 Session idea

**Auto-derive the `docs/current-state.md` PR-lag header from `git log`.** The
plan's own hygiene list notes `docs/current-state.md` lags main by ~26 PRs
(says #421, HEAD is #447) — a hand-maintained header number that drifts every
merge and is only ever caught by a human re-read. A tiny check that parses the
newest `(#NNN)` from `git log --oneline` and warns when the current-state
header's cited PR number trails it by more than a small threshold would turn a
silent-drift doc header into a self-announcing advisory, the same friction→guard
move the model-line lint already makes for cards. Deduped against
`docs/ideas/backlog.md` + the plan's hygiene list: the manual refresh is
listed, an automated staleness guard is not. Captured here for the backlog.

## ⟲ Previous-session review

`.sessions/2026-07-19-testing-store-postgres.md` (PR #446, then the #447 shim
extraction) executed its dual-backend port cleanly and, crucially, its own 💡
— "extract the duplicated dual-backend primitives into one shared module" —
was landed the very next session as `botsite/_db.py` (#447) rather than left to
rot in the backlog. That is the discipline this plan carries forward: #447's
own follow-up (only `testing_store` consumes the new shim;
`submissions_store` still hand-maintains its inline copy) is promoted to
**slice 1** of this cycle instead of being filed and forgotten. The lesson: a
shared-primitive extraction is only half-done until every duplicate consumer is
migrated onto it, so the plan makes "make the shim real" the highest
value-per-effort slice and the baton's top item.
