# Session 2026-07-13 — 2026-07-13-session

> **Status:** `complete` — orchestration session; work landed via PR #241 (merged).

## Close-out (auto-drafted 2026-07-13 — edit, don't author)

<!-- substrate:auto-draft -->

**Evidence (auto-collected — verify, then keep or correct):**

- other touched (1): `HANDOFF.md` — the boot-time trail regeneration,
  untracked by design; not session work.
- git: corrected — the auto-collector saw a detached HEAD with "nothing
  committed" because this was an orchestration session: the real work landed
  via a dispatched worker on branch `claude/clarity-structural-gate` → PR
  #241, squash-merged to main as `1b294d5`. Contents: four per-service
  structural clarity test suites (tests/, botsite/tests/, dashboard/tests/,
  review/tests/), 3 botsite template lede fixes, and the backlog bullet flip.
  Its own session card `.sessions/2026-07-13-clarity-structural-gate.md` is
  complete on main; quality green, live-verified on botsite + dashboard
  `/version`.
- verify: `python3 -m pytest tests/ botsite/tests dashboard/tests
  review/tests -q` → 1006 passed, 1 warning (run on the #241 branch after
  merging main); `python3 bootstrap.py check --strict` → all checks passed
  (re-run on this card's branch after completing the close-out: passes).

**Judgment (the half only the session knows — resolve every slot):**

- Decisions made: (1) the `/prompts` pages are held to the shared idiom only
  (copy-agnostic) to avoid colliding with the in-flight prompts work that
  merged as #236; (2) the 3 botsite lede misses were FIXED in-idiom rather
  than allowlisted — the honesty rule: the allowlist is for
  structurally-different responses only, never for pages that are simply
  below the bar; (3) non-page GET routes are classified in exact-set
  registries, so a new route must be classified or the suite fails.
- Next session should know: the clarity gate lives in
  `tests/test_clarity_structure.py` plus its botsite/tests/,
  dashboard/tests/, and review/tests/ equivalents — any NEW HTML GET route
  must carry the service idiom, and any new parameterized route needs a
  `PARAM_EXPANDERS` entry. FastAPI 0.139 stopped flattening
  `include_router()` into `app.routes`; the suites recurse via
  `original_router` with a trip-wire assert that fires if that shape changes.

## 💡 Session idea

**Teach the kit's auto-draft step about already-landed orchestration
sessions** — the kit auto-drafted this generic
`.sessions/YYYY-MM-DD-session.md` close-out even though the session's work
had already landed a named, complete session card
(`.sessions/2026-07-13-clarity-structural-gate.md`) via a dispatched worker;
this very file sat untracked after PR #241 merged, tripping the stop hook.
The auto-draft step could detect an existing same-day card whose PR merged
and either skip the draft or pre-link it, so orchestrator sessions don't
strand a duplicate card. Deduped against `docs/ideas/backlog.md`: nothing
there touches the auto-draft step or same-day card dedupe.

## ⟲ Previous-session review

`.sessions/2026-07-13-testing-ai-inventory.md` did well: it refused the
minimum three-name fix and documented all 17 genuinely-consumed undocumented
names, and it repaired the upstream card's broken "captured in backlog"
claim instead of just noting it — proven-red-then-green discipline
throughout. One workflow improvement: its downstream count-pin repairs
(chip/badge totals now derived from the registry) were discovered by test
failure, not by search — a close-out step that greps for hardcoded totals
touching a grown inventory would surface those before the red run.

- **📊 Model:** Claude Fable 5 · high · orchestration — coordinator-dispatched session, work executed via workers
