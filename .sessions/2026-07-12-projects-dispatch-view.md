# 2026-07-12 — /projects dispatch view: seat-first index + per-seat copy-ready prompt screen

> **Status:** `in-progress` — branch `claude/projects-dispatch-view`; flips to `complete`
> + PR number as the deliberate LAST code step.

- **📊 Model:** claude-fable-5 · worker (owner dispatch directive) · feature-build

**What this session was about:** Owner single-screen dispatch ask (2026-07-12):
dispatching all 8 fleet seats today means clicking through to GitHub raw files
and hand-copying prompt text. Build the Owner Launch Console on control-plane:
(1) /projects index split into active Seats (owner start order) with retired /
merged stubs collapsed below, (2) a NEW /projects/{package} detail page that
renders every recognized role file's FULL content copy-ready (instructions /
coordinator / failsafe / setup / meta / routine) with the deployed-state badge,
environment name, claude.ai Project link, and a static dispatch checklist —
same TTL-cached github layer, same honest degradation, registry-validated
package names (no traversal). Rung: owner-directed.

## What was done

- (in progress — close-out written at flip)

⚑ Self-initiated: no — owner dispatch directive (2026-07-12).

## 💡 Session idea

(filled at close-out)

## ⟲ Previous-session review

(filled at close-out)
