# 2026-07-14 — ORDER 029: current-state token line + journal quick-ref truing (INC-23/24)

> **Status:** `complete` — branch `claude/order-029-truing-0714`; both
> INC-23 and INC-24 landed, four-suite run 1414 passed, kit gate green
> (word-budget included). PR number added post-open.

- **📊 Model:** Claude Fable 5 · worker · docs truing (boot-set state doc + journal quick reference)

**What this session was about:** order rung — ORDER 029 (control/inbox.md,
INC-23/INC-24). Two boot-set surfaces went stale: (a) `docs/current-state.md`
still says the control-plane GitHub token is UNSET, contradicted by this
repo's own `docs/CAPABILITIES.md` 2026-07-12 entry (the owner set the token —
OWNER-ACTIONS Decided row H); (b) `.session-journal.md` § Quick reference
still describes THREE services and "60 tests" with the three-suite pytest
command — review/ has been live since 2026-07-12 and the real suite is ~1400
tests across four suites.

## What was done

- `docs/current-state.md` — the "GitHub token is currently UNSET" bullet
  replaced with the RESOLVED state: live control-plane runs with a working
  `GITHUB_TOKEN` (authenticated-only cells verified 2026-07-12, per
  `docs/CAPABILITIES.md`; owner set it — OWNER-ACTIONS Decided row H). The
  ORDER 020 durable-PAT pointer kept: a durable contents:write PAT ask
  coexists with "read token works now".
- `.session-journal.md` § ⚡ Quick reference — repo shape trued to FOUR
  services (adds `review/`); test count and command trued to the four-suite
  run (count taken from this session's actual run); run-locally block gains
  the `review.app:app` uvicorn line (port 8082 following the 8000/8080/8081
  convention; module path per `review/Dockerfile`).
- Verified: `python3 -m pytest tests/ botsite/tests dashboard/tests
  review/tests -q` — 1414 passed; `python3 bootstrap.py check --strict` —
  green (born-red hold on this card aside).

⚑ Self-initiated: mostly no — ORDER 029 (INC-23/24). One flagged
decide-and-flag extra: the review/ run-locally uvicorn line in the same
journal section — contained (one line, same section) + reversible.

## 💡 Session idea

**Quick-reference drift check in the kit gate** — `bootstrap.py check` could
grep `.session-journal.md`'s quick reference for the pytest command string
and fail when it diverges from the CLAUDE.md verify line, the way the
word-budget gate already pins `docs/current-state.md`. Worth having because
both INC-23 and INC-24 are the same failure class (a boot-set surface nobody
re-reads after the world changes) and the journal's test command is exactly
machine-checkable. Deduped against `docs/ideas/backlog.md` + queue-state
NEXT: no journal-drift/quick-reference bullet exists. Captured in
`docs/ideas/backlog.md`.

## ⟲ Previous-session review

The testing-import-valve session (PR #320) did well — one-transaction
restore with row-id preservation and honest legacy-column defaults, tested
across all failure lanes; what it missed: its own 💡 concedes orphan-row
backups still import silently (referential pass unbuilt).
