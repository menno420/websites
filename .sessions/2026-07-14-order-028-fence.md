# 2026-07-14 — ORDER 028: restore the capability-seed fence in docs/CAPABILITIES.md

> **Status:** `in-progress` — branch `claude/order-028-fence-0714`; flips to
> `complete` + PR number as the deliberate LAST code step.

- **📊 Model:** Claude Fable 5 · worker · docs fence restore (surgical, content-frozen)

**What this session was about:** order — ORDER 028 (`control/inbox.md`):
`docs/CAPABILITIES.md` lost its substrate-kit `capability-seed` fence markers
(0 grep hits at main `eee8c7a`), so the kit's seat-digest/walls tooling cannot
see the "## Walls — verified blocked" section. Restore ONLY the fence: the
canonical BEGIN/END marker lines copied byte-exact from superbot-next
`docs/CAPABILITIES.md` @ `e2d792a` (lines 19 and 101), wrapped around the
existing walls section with zero content changes; the Append log stays
outside/below the fence.

## What was done

- [in progress — filled at close-out]

⚑ Self-initiated: no — ORDER 028 from `control/inbox.md`.

## 💡 Session idea

**Kit-gate advisory for the capability-seed fence itself** — extend the
`bootstrap.py check` docs scan with an advisory that fails (or at least
flags) when `docs/CAPABILITIES.md` is missing the `capability-seed`
BEGIN/END pair or has them in the wrong order, so the fence can't be
silently dropped again by a future rewrite of the file. Worth having because
that is exactly what happened here — the fence vanished in a content rewrite
and only a manual audit caught it, an order and a full PR later. Deduped
against `docs/ideas/backlog.md` + the queue-state NEXT list: the only
marker-scan bullet ("Kit-gate advisory for exact-ID model lines") covers
session-card model lines, nothing covers the CAPABILITIES fence. Captured in
`docs/ideas/backlog.md`.

## ⟲ Previous-session review

The testing-import-valve session (PR #320) did well — it shipped the restore
half of the lifeboat end-to-end with an 8-test suite covering auth, CSRF,
round-trip byte-exactness, and legacy-backup defaults; what it missed: its
own 💡 concedes the import still silently admits orphan cross-table rows
because SQLite foreign keys stay off, so the valve's "faithful restore"
promise has one known unchecked corruption class.
