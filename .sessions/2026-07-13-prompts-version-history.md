# 2026-07-13 — /prompts: per-seat prompt version history (ORDER 041)

> **Status:** `in-progress`

- **📊 Model:** Claude Fable · worker (order execution) · feature-build

**What this session is about:** ORDER 041 (fleet-manager inbox, dispatched
per ORDER 040 TASK 2) — make the fleet prompt versions BROWSABLE: a version
history per seat (view any version, diff between versions, copy button per
body) sourced ONLY from the fleet-manager registry (git history of
`docs/prompts/v3/per-project/<seat>-{custom-instructions,startup}.md` —
ladder derived dynamically, never hardcoded), the deployed-vs-canonical
drift row made version-aware, history reachable two clicks from the site
root. Contained bonus: pin seat 9 (`curious-research`, added upstream in
v3.6) into `app/roster.py`. Rung: order — ORDER 041.

## What was done

(in progress)
