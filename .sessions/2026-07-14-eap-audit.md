# 2026-07-14 — EAP project close-out audit

> **Status:** `complete` — PR #332, branch `claude/eap-audit-0714`;
> `docs/audits/eap-project-audit-2026-07-14.md` landed with a new
> `docs/audits/README.md` index (reachability root) linking both audits.

- **📊 Model:** Claude Fable 5 · medium · review/verify (EAP close-out docs audit)

**What this session was about:** Land `docs/audits/eap-project-audit-2026-07-14.md`
— the seat's definitive EAP close-out audit (owner directive 2026-07-14, relayed
by the coordinator). Work-ladder rung: order/owner directive (EAP closes
2026-07-14; this is the close-out deliverable).

## What was done

- `docs/audits/eap-project-audit-2026-07-14.md` — the close-out audit, 11
  numbered sections: identity & scale (201 sessions / 353 commits / 332 PRs:
  316 merged, 5 closed-unmerged, 11 open / 117 backlog bullets / 1414 tests);
  tooling verdicts; the 13-row walled-capabilities table with verbatim
  denials; merge & landing friction (median 3.1 min, worst #163 8.2 h, 47%
  fail→pass with the born-red HOLD inflation caveat, #321→#324 race, #307→
  #310/#314 false-green); scheduling & wake friction (3 bake schedule fires
  ever, zero missed days, all +2–3 h late; smoke-crawl run 29308117901 fired
  late not wedged; send_later 84/85 clean then the 07-12 incident's 9
  dropped); environment & platform (state.json churn freshest `555208d` THIS
  session; MCP ~25-min staleness; #328 403-vs-404 masking); ceremony cost
  (born-red paid: 63-PR clean streak; heartbeat pins caught #307 in 15 min);
  self-fixes (9 merged + #324 parked + 5 extras); top-5 ranked pains with
  paste-ready ANTHROPIC asks; net-new wishlist; honest gaps (container
  deaths, two LEADs, the ~197 unread job logs, #281 open BY DESIGN). Every
  §2–§9 finding carries a FLEET-FIX / ANTHROPIC / ACCEPTED disposition.
- `docs/audits/README.md` — new audits index (badge `reference`); READMEs
  under docs/ are reachability roots per `bootstrap.py check_reachable`, so
  this is the real read-path link for the `audit`-badged report (the 07-13
  audit was only exempt via its `historical` badge).
- `docs/ideas/backlog.md` — this session's 💡 captured (zero-commit-branch
  synthetic pin for the claims-drift gate).
- `control/claims/2026-07-14-eap-audit.md` deleted at close (this commit).
- Verified: `python3 -m pytest tests/ botsite/tests dashboard/tests
  review/tests -q` — 1414 passed at the flipped final state;
  `python3 bootstrap.py check --strict` — green at the flipped state (the
  only red during the session was the designed born-red hold on this card,
  plus one `[stamp]` doc-hygiene finding fixed pre-commit by citing the
  decisions ledger without duplicating the decision ID).

⚑ Self-initiated: no — owner directive 2026-07-14 (EAP close-out audit), relayed
by the coordinator.

## 💡 Session idea

**Synthetic zero-commit-branch pin for the claims-drift gate** — Lane 1
(`git merge-base --is-ancestor`, `tests/test_claims_drift_gate.py:99`) is
trivially TRUE for a claim whose branch has zero unique commits, so the gate
would call a brand-new claim "merged"; the coordinator-relayed "#319 false
positive" was NOT found in the repo record (audit §11), and one synthetic
test would either pin the bug or retire the lead. Worth having because the
gate is one day old, already load-bearing in the control fast lane, and this
is its one alleged failure the synthetic suite doesn't cover. Deduped against
`docs/ideas/backlog.md`: the only claims-gate bullet (PR-number claim token)
covers the pruned-ref lane, not this one. Captured in `docs/ideas/backlog.md`.

## ⟲ Previous-session review

The testing-import-valve session (PR #320) did well — it shipped the restore
half of the lifeboat with real fail-closed auth/CSRF/size tests and honest
`.get`-default handling for legacy backups, and its 💡 (referential orphan
check) named the exact corruption class its own validation still admitted;
what it missed: it verified the round trip only through the fresh exporter,
leaving no frozen real-export fixture, so compatibility with bytes OLDER code
actually wrote stayed unproven (picked up by the later round-trip-pin work).
