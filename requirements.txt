# Control-plane site runtime deps — pinned (see docs/site.md, D-0003).
fastapi==0.139.0
uvicorn==0.51.0
jinja2==3.1.6
httpx==0.28.1
Markdown==3.10.2
# Sanitizes rendered-markdown HTML to an allow-list (journal browser, D-0014).
# Lazy-imported with a graceful fallback in app/journal.py, but pinned so CI
# installs it and the sanitizer path is always exercised.
bleach==6.2.0
# Form parsing for the gated /owner POST actions (rerun-ci). Runtime dep.
python-multipart==0.0.32
