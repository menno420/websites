__pycache__/
*.pyc
.pytest_cache/
.env

# substrate-kit local guard-fire telemetry (per-run, not source)
.substrate/guard-fires.jsonl

# substrate-kit handoff pointer (v1.11.0+): regenerated UNTRACKED at every
# session boot — read it, never commit it (kit machinery, not source).
HANDOFF.md

# tester-program local SQLite (ephemeral runtime state — never commit data)
botsite/testing.sqlite3*

# owner-writeback local SQLite audit log (ephemeral runtime state — ORDER 020)
app/writeback.sqlite3*
