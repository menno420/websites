# websites — decisions

> **Status:** `living-ledger`
>
> Generated by substrate-kit. Append-only decision ledger — entries are
> superseded, never deleted. Rule docs cite entries as bare [D-NNNN] ids;
> this file holds the provenance so rules never narrate it inline.

<!-- Grammar: ## [D-NNNN] <title> / - status: decided|superseded|retired / - date: YYYY-MM-DD / - supersedes: D-NNNN (opt) / - superseded-by: D-NNNN (opt) / - verdict: <one line> / - why: <2-3 lines> / - provenance: <ref> -->

## [D-0001] Adopt the substrate-kit workflow

- status: decided
- date: 2026-07-09
- verdict: websites runs on the substrate-kit agent workflow.
- why: A repo-resident working agreement, decision ledger, and session
  discipline let agents work correctly with little steering; adopting the
  kit starts websites governed instead of accreting rules ad hoc.
- provenance: substrate-kit adoption interview

## [D-0002] Adoption provenance pin
- status: decided
- date: 2026-07-09
- verdict: Adopted fresh-from-kit: menno420/substrate-kit main @ 08a291f96599d5b7ec1b6a46a381d4e3f88839c0, dist/bootstrap.py blob 7d2b85c740b952aa8c52d013fdcfa182f395b509 (643694 bytes, stdlib-only), command: python3 bootstrap.py adopt.
- why: Pin exactly which kit build planted this repo so later upgrades diff against a known source; mirrors superbot-next's D-0002. Never a copy of superbot.
- provenance: superbot docs/planning/websites-project-kickoff-2026-07-09.md (PR #1876), sequence step 1

## [D-0003] Control-plane site stack: FastAPI + Jinja2 + httpx, server-rendered, no build step
- status: decided
- date: 2026-07-09
- verdict: The control-plane site is a single server-rendered FastAPI app (Python 3.12, Jinja2 templates, httpx client, Markdown renderer), pinned deps, simple Dockerfile binding 0.0.0.0:$PORT.
- why: Boring and Railway-friendly; matches the inherited taste from superbot's dashboard/ (small FastAPI apps, minimal deps, honest data); no frontend build step to break. Async httpx lets one page fan out ~30 GitHub calls concurrently.
- provenance: websites PR #2 (control-plane-site session, 2026-07-09); kickoff brief 'stack: builder's free choice — decide and flag'

## [D-0004] Control-plane data/auth/cache model
- status: decided
- date: 2026-07-09
- verdict: All GitHub data fetched live server-side: REST API for listings/state (GITHUB_TOKEN), raw.githubusercontent.com WITHOUT auth for public file bodies; per-URL in-memory TTL cache 180s caching successes and stable negatives (404/403/401) but never transient errors; ?refresh=1 busts. Auth = HTTP Basic, any username, password compared constant-time to SITE_PASSWORD (fail closed if unset); /healthz unauthenticated. Every fetch degrades per-cell — the board renders 'unknown (reason)' instead of faking or 500ing (e.g. secrets without admin scope, rulesets without scope).
- why: 3-min TTL keeps a board load ~30 API calls well under PAT rate limits while staying near-live; raw host needs no token for public repos but 404s if sent a bad bearer (found live); shared-secret Basic auth is exactly the 'something simple' the kickoff asked for.
- provenance: websites PR #2 (control-plane-site session, 2026-07-09)

## [D-0005] Deployment target: dedicated Railway project `superbot-websites`
- status: decided
- date: 2026-07-09
- verdict: The site deploys as service `control-plane` in its own fresh Railway project `superbot-websites` (IDs in docs/deployment.md), repo-connected to menno420/websites@main — merge to main IS the deploy. Never deployed into, or configured via, the production bot project.
- why: Hard isolation from the live bot (the ambient Railway IDs in agent containers point at production — a standing footgun); repo-connect makes redeploys zero-step and keeps the forward-only-git flow the sole change path.
- provenance: websites PR #3 (railway-deploy session, 2026-07-09); kickoff brief Phase 3

## [D-0006] Durable owner PAT wired as the control-plane service token — board fully live
- status: decided
- date: 2026-07-09
- verdict: A durable owner GitHub PAT is set as the `control-plane` service token in the `superbot-websites` Railway project (variable upsert via the public GraphQL API, RAILWAY_API_KEY only, ambient production-bot IDs never passed, no destructive mutation). The board now runs on the authenticated GitHub rate limit; the auth-gated cells (actions secret names, auto-merge, CODEOWNERS presence) render live and were verified on the deployed site. Per-cell degradation still stands for anything the token scope or egress can't reach — the board never fakes a value.
- why: Closes the one outstanding owner TODO from the deploy session; lifts the board off the unauthenticated rate limit and unlocks the secrets / auto-merge / CODEOWNERS cells so the oversight board actually shows the config state it exists to surface.
- provenance: websites PR #6 (token-wiring session, 2026-07-09); supersedes the owner-TODO left open by [D-0005]

## [D-0007] Rework plan for dashboard/ + botsite/ drafted, awaiting owner review
- status: decided
- date: 2026-07-09
- verdict: The step-3 rework of superbot's dashboard/ and botsite/ into this repo is planned in docs/planning/dashboard-botsite-rework-plan-2026-07-09.md — plan only, no code ported, live sites untouched. Recommended shape: keep ideas/functionality, rebuild implementation on this repo's substrate; ONE repo with MULTIPLE Railway services (not one shared FastAPI process) so the public botsite stays secret-isolated from the private control-plane/dashboard; botsite reworked first, dashboard second; consume superbot's committed JSON artifacts via raw GitHub (read-only, forward-only). Seven open questions routed to the owner (public/private per site, exact-design-vs-restyle, merge-vs-separate, control-panel coupling to the live bot, submissions pipeline, domains, data feed).
- why: The kickoff makes step 3 plan-first — describe the plan before starting the rework — because it touches tools the owner uses today. This entry records that the plan exists and is the gate; implementation waits on owner review of the open questions (esp. Q4, the control panel's coupling to the running production bot). Numbered D-0007 (not D-0006) because a concurrent token-wiring session claimed D-0006 in flight the same day.
- provenance: websites PR #4 (dashboard-botsite-rework-plan session, 2026-07-09); superbot docs/planning/websites-project-kickoff-2026-07-09.md sequence step 3

## [D-0008] Botsite rebuilt + deployed as a second Railway service (public, server-rendered, ds/-styled)
- status: decided
- date: 2026-07-09
- verdict: The public bot site is rebuilt on this repo's substrate under `botsite/` (server-rendered FastAPI + Jinja2, no build step) and deployed as a NEW Railway service `botsite` (ID 4314f839-0a93-4995-b424-02861ad2d5e6) in the existing `superbot-websites` project, alongside `control-plane`, dark-launched at https://botsite-production-cfd7.up.railway.app (Root Directory = `botsite`, own Dockerfile, repo-connected to main = merge deploys). It reproduces superbot botsite's public surface (Home/Features/Features-detail/Commands/Games/Changelog/Status/Design/Submit + /healthz + /palette.json), styled with the promoted `ds/` design system (tokens.css + components.css verbatim, plus ds.js). Data is superbot's committed `site.json` consumed live via raw.githubusercontent.com with a 180s TTL cache — read-only + forward-only toward superbot, never faking data (a failed feed shows an honest banner). The v1/v2 SPA duality and Tailwind-CDN are dropped for one server-rendered site on one design system. `/submit`'s DB write path is a clearly-labeled stub until the submissions Postgres is provisioned.
- why: Executes rework-plan step 3 (botsite half, [D-0007]) with the plan's recommended shape: keep ideas/functionality, rebuild the implementation; one repo, multiple Railway services so the public marketing surface stays secret-isolated from the private control-plane; consume the committed JSON artifact rather than rebuilding the export tooling here (plan Q7). Public/server-rendered/ds-styled resolves plan Q1/Q2 with the reasonable defaults; submissions (Q5) and custom domains (Q6) stay deferred to the owner. Created with RAILWAY_API_KEY + explicit superbot-websites IDs only; ambient production-bot IDs never passed; no destructive Railway mutation.
- provenance: websites PR #7 (rework-botsite session, 2026-07-09); docs/botsite.md; docs/planning/dashboard-botsite-rework-plan-2026-07-09.md

## [D-0009] Dashboard rebuilt + deployed as a third Railway service (private, read-only oversight; live-bot control panel stubbed by design)
- status: decided
- date: 2026-07-09
- verdict: The developer dashboard is rebuilt on this repo's substrate under `dashboard/` (server-rendered FastAPI + Jinja2, no build step) and deployed as a NEW Railway service `dashboard` (ID 39007299-11a2-49a8-9c5c-21e17194fb3e) in the existing `superbot-websites` project, alongside `control-plane` and `botsite`, dark-launched at https://dashboard-production-a91b.up.railway.app (Root Directory = `dashboard`, own Dockerfile, repo-connected to main = merge deploys). It is PRIVATE: every route is gated behind HTTP Basic auth (any username + SITE_PASSWORD, constant-time; fail-closed 503 if unset), same pattern as control-plane; /healthz stays unauthenticated for Railway. The read-only oversight surface is fully live — Overview / Functions / Commands / Aliases (client-side collision-checker) / Settings / Access / Env-map (names + locations only, never values) / Ideas / Bugs / Updates / Console / Status + /palette.json — reproducing superbot dashboard's read-only pages, styled with the vendored `ds/` design system. Data is superbot's committed `dashboard.json` + `console.json` consumed live via raw.githubusercontent.com with a 180s TTL cache — read-only + forward-only toward superbot, never faking data (a failed feed shows an honest banner). THE ONE DELIBERATE STUB: superbot's Discord-OAuth control panel writes the LIVE PRODUCTION BOT's control API (settings/help/cog-routing applied live) plus an owner-gated submissions-moderation ring; that live-write path couples websites to the running production bot across the repo boundary and is owner-decision Q4, so it is NOT wired here. `/admin` renders the UI shape but every mutating action is a disabled placeholder ("requires owner wiring — not connected to the live bot"), and NO production bot control-API URL or token exists anywhere in the service (a test asserts the literal names never appear).
- why: Executes rework-plan step 3 (dashboard half, [D-0007]) with the plan's recommended shape: keep the read-only ideas/functionality, rebuild the implementation; one repo, multiple Railway services; consume the committed JSON artifacts rather than rebuilding the export tooling here (plan Q7). Private-Basic-auth resolves plan Q1 (the oversight data is internal, so gate it like control-plane). The live-bot-write control panel stays a labeled stub because carrying it would wire websites to the running production bot — an owner call (Q4) that must not be made implicitly; the safe, reversible default is to ship the read-only surface live and stub the mutating path with zero production credentials present. Created with RAILWAY_API_KEY + explicit superbot-websites IDs only; ambient production-bot IDs never passed; no destructive Railway mutation. The live superbot dashboard keeps running unchanged.
- provenance: websites PR #8 (rework-dashboard session, 2026-07-09); docs/dashboard.md; docs/planning/dashboard-botsite-rework-plan-2026-07-09.md

## [D-0010] console.json consumed under a pinned, versioned cross-repo shape contract
- status: decided
- date: 2026-07-09
- verdict: The `/console` page's feed (superbot's committed `botsite/data/console.json`) is no longer consumed on an implicit schema. Superbot commits the canonical versioned contract (`botsite/data/console_data_contract.json`, superbot PR #1884: top-level families + guaranteed fields per record + `version`), stamps `meta.schema_version` into every emitted feed, and enforces producer-side parity + fail-closed shape checks in its CI. This repo pins the contract copy it was built against (`dashboard/console_data_contract.json`, v1) and verifies at render time (`data_source.console_contract_issue`): version mismatch or a missing contracted family renders an honest schema-drift banner on `/console` (never-fake-data posture) instead of a silently wrong page. Shape changes flow as: superbot edits the canonical contract + bumps version (explicit, reviewable) → this repo syncs the pinned copy + adapts templates/fixture in one commit.
- why: The two repos shared an implicit schema — a producer-side family/field rename would silently blank or corrupt the consumer page (superbot's BUG-0022 desync class) with no signal on either side. The first pass of writing the consumer against the real contract immediately proved the point: the console page treated `ideas`/`bugs` as lists (`|length`) while the feed ships counter dicts (`{total, by_status, open_count, open}`), so its stat tiles showed dict-key counts — fixed in the same PR (template + route + fixture now match the contracted shape, plus an Open-bugs list the dict shape provides for free). Enforcement is deliberately split: field-level checks live producer-side in superbot's CI (where the shape is constructed); the consumer runs the two cheap render-time checks that make drift visible where the owner looks.
- provenance: websites PR #11 (console-feed-contract session, 2026-07-09); superbot PR #1884 + superbot `.sessions/2026-07-09-kl6-console-telemetry.md` (the idea's origin); docs/dashboard.md § Data source

## [D-0011] Control-plane + dashboard made PUBLIC (Basic auth dropped); readiness secrets cell masked to a count
- status: superseded
- date: 2026-07-09
- superseded-by: D-0012
- verdict: The two formerly-private websites services are now **public** — the HTTP Basic auth gate is removed from both `control-plane` (`app/main.py`) and `dashboard` (`dashboard/app.py`). Every route serves without credentials; the `_authorized` helper + `auth_gate` middleware + now-dead `base64`/`secrets` imports are deleted, `/healthz` still works, and the apps no longer fail-closed 503 when `SITE_PASSWORD` is absent (they run fully public). `SITE_PASSWORD` is left set-but-unused in Railway (non-destructive, reversible); the dead `dashboard/config.py` (which only held it) is removed and `app/config.py` drops the var. The **one security refinement**: because the control-plane readiness board renders GitHub Actions **secret NAMES** (obtainable only with an admin-scope token — the single non-public datum on an otherwise public-repo-derived board), the secrets cell now shows a **COUNT only** (`N secret(s)`). The individual names are never rendered in the board HTML nor serialized into `/api/readiness.json` (the raw secrets API response is no longer carried in the returned row). Everything else on the board (rulesets, required checks, CODEOWNERS presence, auto-merge, open PRs, journal contents) is derived from public repos and is unchanged. Tests rewritten for the public model; a new assertion verifies a secret name (`ANTHROPIC_API_KEY`) is absent from the served board HTML and JSON. `botsite` was already public and is untouched. Deployed non-destructively to the `superbot-websites` project only (RAILWAY_API_KEY + explicit superbot-websites IDs; ambient production-bot IDs never passed; no destructive mutation).
- why: The owner directed it in-session, verbatim "Yes drop the auth" — the passwords were judged unnecessary friction, and the surfaces are read-only oversight of public data (superbot's committed JSON artifacts + public repo config), so there is nothing to gate. The single genuinely non-public datum, the Actions secret names, is masked to a count rather than dropped so the board keeps its "N secrets configured?" signal without exposing what they are. Owner-directed work merges immediately on green (Q-0191-class); the change is reversible (re-add the middleware / the var is still set) and covered by tests.
- provenance: websites PR #12 (drop-auth session, 2026-07-09); owner verbatim "Yes drop the auth"; docs/site.md § Auth, docs/dashboard.md, docs/deployment.md

## [D-0012] Password-gated `/owner` area on control-plane (public site unchanged; full detail + real actions)
- status: decided
- date: 2026-07-09
- supersedes: D-0011
- verdict: The control-plane main site stays 100% public and byte-identical (the D-0011 masking is preserved — the public board and `/api/readiness.json` still show an Actions-secrets COUNT only, and a test asserts zero secret names in the public HTML/JSON). A single gated area `/owner` is added (`app/owner.py`, an included `APIRouter(prefix="/owner")` — the public routes in `app/main.py` are untouched). The gate is HTTP Basic (any username), password compared constant-time (`secrets.compare_digest`) to `SITE_PASSWORD`: no/wrong creds → 401; correct → 200; `SITE_PASSWORD` UNSET → the `/owner*` routes fail closed with 503 while the public site keeps serving. `SITE_PASSWORD` (left set-but-unused on the service since D-0011) is read again, but ONLY by the owner router. `/owner` renders the readiness board UN-masked — the real Actions secret NAMES per repo, threaded exclusively through a new `readiness.board(reveal_secrets=True)` path that the public `board()` call never sets (so the public output cannot regress) — plus broken-check and oldest-PR detail; `/owner/api/readiness.json` is the authed JSON with names. Privileged actions (POST, same gate, reversible, using creds already on the box): `refresh` clears the in-memory TTL cache in-process; `rerun-ci` looks up the latest FAILED Actions run on a selected repo's default branch and POSTs `rerun-failed-jobs` via the existing `GITHUB_TOKEN`, degrading honestly on 403/no-failed-run. Added `python-multipart` (pinned) for the POST form. DELIBERATELY NOT WIRED, pending separate owner approval: any Railway account-token action and any live production-bot control API — no `RAILWAY_API_KEY` is present in the service env, and no bot control-API URL/token exists anywhere in the app.
- why: The owner directed it in-session, verbatim "we could add a password to a secure part of the site, one that gives you actual power to do something and view all secrets etc, but the main site should always be browsable." This is a targeted, reversible refinement of D-0011: D-0011 dropped auth from the whole site and masked secret names to a count; this keeps that public posture intact but adds ONE gated overlay for the detail + power the owner wants, rather than re-privatizing anything. A per-router gate (not global middleware) is the simplest solid option that guarantees the public surface stays credential-free and byte-identical. The two genuinely powerful-and-external levers (Railway account actions, live-bot control) are left unwired on purpose — they cross into irreversible/production territory that is its own owner decision, so the safe default is the read-detail + reversible-actions surface shipped here with zero production-control credentials present. Owner-directed → merges immediately on green; fully reversible (remove the router / the gate is additive).
- provenance: websites PR #14 (owner-area session, 2026-07-09); owner verbatim intent above; docs/site.md § "Public site + gated /owner area"

## [D-0013] Engage the substrate-kit machinery (render docs, CI gate, backfill, session loop)
- status: decided
- date: 2026-07-09
- verdict: The kit adoption (PR #1) was ritual-clean but never engaged; this pass turns it live. Rendered the 8 planted binding docs for real (filled architecture_layers, ownership_model, new_area_ownership, mutation_seam, drift_resolution, staleness_review, owner_profile, review_ritual from the project config via 'bootstrap render --live') and hand-filled their core tables so no UNRENDERED-SLOTS banner remains. Installed the kit CI gate .github/workflows/quality.yml (Python 3.12; 'bootstrap.py check --strict --require-session-log' + the three app pytest suites, on PRs and pushes to main). Backfilled bookkeeping: a retro .sessions/ card for PR #10 and PRs #4/#10/#13/#15 into current-state Recently-shipped. Routed the 7 rework-plan open questions into docs/question-router.md as Q-0001..Q-0007. Engaged the kit session loop: ran session-close (mined reflections + rebuilt .substrate/episodic_index.json over 12 logs) and reconciled session_count 0 -> 12 to match the indexed session logs. Seeded .session-journal.md with a real quick-reference. Vendored bootstrap.py is byte-identical to substrate-kit dist HEAD, so no engine bump.
- why: Adoption planted the docs and checkers but nothing ran them: the binding docs were template shells with slot banners, no CI enforced the kit check, the session counter sat at 0, and merged PRs had drifted from the ledger. Engaging the machinery is contained, reversible, and forward-only, and it makes the workflow self-enforcing (the CI gate + born-red card ritual) instead of exhortative.
- provenance: websites PR (engage-kit session, 2026-07-09); owner-directed kit-hygiene task

## [D-0014] Journal browser upgrade (sanitized markdown rendering + cross-repo search) + mobile-responsive control-plane
- status: decided
- date: 2026-07-09
- verdict: The control-plane journal browser (`app/journal.py` + `app/main.py` + templates) gains two capabilities and the whole control-plane app gains mobile polish. **(A1) Safe markdown rendering:** `.md` journal files (session logs, ledgers, routers) render to real HTML via a new `journal.render_markdown()` that **lazy-imports** `markdown` (extensions `fenced_code`, `tables`, `sane_lists`) and **sanitizes** the output with `bleach` (pinned `bleach==6.2.0`) to a tag/attribute allow-list; both imports degrade gracefully (missing `markdown` → escaped `<pre>`; missing `bleach` → unsanitized render, since the source is trusted repo content) so the page never 500s. The old non-lazy module-level `import markdown` in `app/main.py` is removed. **(A2) Cross-repo search:** `GET /journal/search?q=` (HTML) and `GET /journal/search.json` (JSON) run one case-insensitive query across **all four repos'** journal corpus — the configured ledgers/routers plus the most-recent `SEARCH_SESSION_LIMIT=25` session logs per repo — fetched through the existing TTL cache (raw-host bodies, no token burn), ranked by match count, each hit carrying repo · path · line · a `<mark>`-highlighted (XSS-safe, fully escaped) snippet + a GitHub deep-link (`…#L{n}`) + an internal file link. Fetch failures collect into an `errors` list rendered as an honest banner instead of silently dropping a repo. A global search box lives in the header (`base.html`). **(B) Mobile:** `base.html`'s inline stylesheet gets a `@media (max-width: 640px)` block — wide board tables get a `min-width` and scroll horizontally inside their `overflow-x:auto` card rather than squashing; header/nav/search reflow full-width with tap-friendly targets; desktop rules only added-to, never changed. **Board fix:** the readiness board's `websites` row `expected_required_checks` moves `[] → ["quality"]` now that the kit `quality` gate is a REQUIRED check on `main` (owner set the ruleset; verified live on PR #18 — `mergeable_state=blocked` with the `quality` check `in_progress`, which a non-required check would not produce). The board already derives the *configured* required checks live from the ruleset, so the row now shows `quality` as both configured and expected.
- why: The journal browser is the owner's read surface onto the estate's process memory; raw-ish markdown and no search made it a file dump. Rendering (safely) + a cross-repo grep turn it into something you actually read and query. Lazy-import + graceful fallback keeps the render path from ever taking the whole app down over a dependency, and bleach sanitization is defense-in-depth even though the docs are trusted (a `<script>` a doc happens to contain is stripped). Fetch-and-grep over the TTL cache (rather than the GitHub code-search API) is deterministic, testable offline, and burns no API budget (raw host, cached). Mobile polish is pure addition behind one media query — the owner browses from a tablet/phone and the board's 4-column tables were unusable at 375 px. The `websites` required-check fix closes real drift the owner flagged: the ruleset is now live, so the ledger side had to catch up or the row lied. All contained, reversible, forward-only, test-covered (8 new tests: render produces HTML elements, sanitizer strips `<script>`, cross-repo ranked search, error-banner path, empty query, route-order registration, and the board expectation).
- provenance: websites PR #18 (journal-search-mobile session, 2026-07-09); owner-directed improvement batch; docs/site.md § "Journal browser" / "Mobile / responsive"; STEP 0 required-check verification captured in the session log

## [D-0015] Hardening + verification pass (Railway-ID guard, healthcheck, stub-label strengthen, OWNER-ACTIONS)
- status: decided
- date: 2026-07-09
- verdict: A hardening batch that closes the #16 definition-of-done and adds enforcing/reusable safety tooling. **(P0)** Verified live on `main`: `.substrate/state.json` `session_count` = 12; **zero** `UNRENDERED` banners or unfilled `${…}` slots across the 8 binding docs (CONSTITUTION.md, docs/architecture.md, ownership.md, runtime_contracts.md, collaboration-model.md, AGENT_ORIENTATION.md, owner-profile.md, ai-project-workflow.md); `.github/workflows/quality.yml` green on head `f2020c6`. **(P1a)** Added `scripts/check_no_ambient_railway_ids.py` — a deterministic, pure-stdlib guard that fails CI if any tracked non-markdown file *reads* one of the three ambient production-bot IDs (`RAILWAY_PROJECT_ID` / `RAILWAY_SERVICE_ID` / `RAILWAY_ENVIRONMENT_ID`) from the environment, AND asserts a safety doc carries the loud warning. Wired as a step in `quality.yml`; covered by `tests/test_check_no_ambient_railway_ids.py` (clean pass + planted-violation fail + safe-explicit-ID non-match). Loud `docs/RAILWAY-SAFETY.md` (`binding`) + a warning block in `docs/deployment.md`, linked from `docs/current-state.md`. **(P1b)** Verified both stubs are unmistakably labeled against live served HTML; strengthened the botsite `/submit` form with a visible "Stub — not wired" `sb-badge` + an explicit "does not store or send" note beside the only clickable mutating control (dashboard `/admin` already ships disabled `aria-disabled` controls + "(stub) / not connected to the live bot"). **(P1c)** Added `scripts/healthcheck.py` — reusable 3-URL post-deploy check (`/healthz` + `/` on all three services); ran it live, all six endpoints 200. **(P2)** Verified the live readiness board renders real signals (rulesets, required checks + live check-run state, CODEOWNERS presence, secrets count, auto-merge, open-PR health) for all four repos — no blank/unknown-where-data-exists; the only non-green cells are legitimately real states (superbot + websites have no CODEOWNERS file; substrate-kit required rows read "missing" because its ruleset check names don't match the live run names on a mid-run head; superbot-next `report` is red-by-design annotated). No `app/readiness.py` fix needed. Seeded `docs/owner/OWNER-ACTIONS.md` (`owner-guidance`) — the living owner-decision list.
- why: The kit was engaged (D-0013) but the ambient-Railway-ID rail lived only in prose and the maintainer's standing instruction, so a future session could wire it back by accident; converting it to an enforcing CI guard (friction → guard) makes the system catch its own mistake. The healthcheck turns the ad-hoc post-merge curl into one reusable command. Strengthening the `/submit` label removes the last ambiguity where a clickable control could read as working. OWNER-ACTIONS gives the owner one skimmable place for the decisions only he can make. All contained, reversible, forward-only, test-covered; no production credential introduced and no Railway/production mutation performed.
- provenance: websites PR #19 (harden-verify session, 2026-07-09); owner-directed hardening + verification batch; docs/RAILWAY-SAFETY.md, docs/deployment.md, docs/owner/OWNER-ACTIONS.md

## [D-0016] Botsite content depth — per-command detail pages + enriched changelog

- status: decided
- date: 2026-07-09
- verdict: The public botsite gains content depth without a new dependency or secret. **(1) Per-command detail** `GET /commands/{name}` (`command_detail.html`) renders everything the committed `site.json` feed actually holds for a command — invocation `!name`, the bot's own one-line description, category/area, permissions, status, aliases, examples, and linked design ideas — plus a same-area "More {area} commands" cross-link block. It reuses the TTL-cached `data_source.commands()` shaping (a detail page can never drift from its list row) via new pure helpers `command_by_name()` / `related_commands()`; absent fields (the feed leaves `cooldown`/`use_cases`/`notes` null and most rows carry no aliases/examples/linked-ideas) are **omitted cleanly, never invented**. Unknown names → **404** through a generalized `not_found.html`. Every `/commands` list row and `feature_detail.html` area row now **links to the detail page**, and `/palette.json` command entries deep-link the same way; links are built with `command_href()` which **percent-encodes URL-reserved names** (superbot ships one, `+prize`, now resolving at `/commands/%2Bprize`). **(2) Enriched changelog** (`changelog.html`): the real artifact is `site.json`'s `bot_changelog` — genuine but thin (a few entries, **no semantic version numbers**), so the page deepens it honestly rather than fabricating releases — a "Latest build" panel from the real `meta.build` (commit + subject + date) and `counts` via `changelog_context()`, entries grouped by kind (added/improved/fixed) via `changelog_by_kind()`, the newest-first timeline retained, and a footer stating the sourcing (`bot_changelog` + `meta.build`). Tests +9 (botsite suite 23; all three CI suites 80 passed): detail renders real fields, URL-safe `+prize` resolves, unknown 404s, absent fields omitted, list/feature rows link to detail, changelog build-panel + grouped kinds + sourcing note, changelog honest-banner degrade path.
- why: A reference site with single-line command rows and a 3-entry changelog is a directory, not a reference — content depth is the point of the estate's public surface. Reusing the existing `commands()` shaping + TTL cache keeps detail pages fast and drift-proof; percent-encoding the one reserved command name (`+prize`) is the friction→guard for the URL-safety footgun. The changelog stays inside the never-fake-data rail: real version history genuinely does not exist in the feed, so instead of inventing one the page surfaces the real build metadata and groups the real entries, with a footer naming the source. No new runtime dep (the changelog entries are plain sentences, not markdown — botsite stays lean and secret-free), no Railway/production mutation. All contained, reversible, forward-only, test-covered.
- provenance: websites PR #21 (botsite-content-depth session, 2026-07-09); owner-directed content-depth batch; docs/botsite.md § Routes / Data source (command detail + changelog source)

## [D-0017] Fix the leaky born-red session gate — adopt the upstream substrate-kit v1.0.0 fix

- status: decided
- date: 2026-07-09
- verdict: The born-red session-gate leak that let **PR #19 auto-merge an effectively-empty PR on its `in-progress` card alone** is closed by an **upstream ADOPTION** (not a local fork). The vendored `bootstrap.py` was a **pre-1.0.0** kit build with two holes: **Hole #1** — `check --strict --require-session-log` only verified the required session-card MARKERS were present, never the Status VALUE, so a born-red (`in-progress`) card passed strict; **Hole #2** — the "current" card was selected newest-by-mtime, but a fresh CI checkout flattens every file mtime to checkout time, so an OLD `complete` card could be picked over THIS PR's `in-progress` one and mask it. Both are fixed at kit **v1.0.0**: **(1)** re-vendored kit-main HEAD `dist/bootstrap.py` (kit commit `eb540d9`; old md5 `85884a8c…`, no `KIT_VERSION` → new md5 `af8418e0…`, `KIT_VERSION = "1.0.0"`). Its checker adds `status_in_progress()` — a card whose Status badge carries `in-progress`/`in progress`/`wip`/`hold`/`drafted` fails `--strict` and is **never allowlistable** (hole #1). It adds `check --session-log <file>` explicit card selection — the diff-aware selection CI derives from the PR diff, replacing the mtime guess (hole #2). **(2)** Recorded `"kit_version": "1.0.0"` in `substrate.config.json` (the only config change the new engine needs; `check --strict` otherwise reads the existing config clean — no migration). **(3)** Folded the kit's **exact generated** diff-aware step (byte-faithful to the engine's `live_ci_workflow`, captured from a throwaway `adopt --wire-enforcement` run) into the existing single `quality` workflow — deriving `card` from `git diff` under `.sessions/` and passing `${card:+--session-log "$card"}`, with `fetch-depth: 0` on checkout — **rather than adding the kit's separate `substrate-gate.yml`**, so there is exactly ONE required enforced session-log path and no double/conflicting logic; the Railway-ID guard + pytest steps are untouched. **Both directions proven empirically** with the #19 repro (verbatim in the session log) and encoded as a **regression test** (`tests/test_born_red_session_gate.py`, 4 tests): an `in-progress` card selected diff-aware → **exit 1 FAIL** (even with a stale `complete` card the newest by mtime), the same card flipped `complete` → **exit 0 PASS**. Full suite green (84 passed); `check --strict --require-session-log` green on the completed branch; Railway-ID guard green.
- why: A born-red gate that doesn't actually hold the merge is worse than none — it reads as protection while letting empty PRs land (the #19 failure). This is a friction→guard fix: convert the leak into an enforcing check. Adopting the upstream-tested v1.0.0 engine (rather than hand-patching the vendored copy) closes both holes with code the kit already tests both directions, and keeps this repo a faithful kit **consumer** — the fix travels from upstream, it isn't forked here. Folding into `quality` (not a second workflow) keeps a single required gate the owner already made a required status check. The **upstream substrate-kit repo fix is handled by a SEPARATE session** — substrate-kit is untouched here (read-only reference), so the two sessions don't collide. Contained, reversible, forward-only, CI/tooling-only (no app code → no Railway redeploy needed for behavior); no production credential added, no production mutation.
- provenance: websites PR (fix-born-red-gate session, 2026-07-09); coordinator-relayed owner direction; kit-main `dist/bootstrap.py` @ `eb540d9` (`status_in_progress` + `check --session-log`); `.github/workflows/quality.yml` (diff-aware session-gate step); `tests/test_born_red_session_gate.py`

## [D-0018] Deploy-state drift cell + `/version` endpoints (ORDER 001)

- status: decided
- date: 2026-07-09
- verdict: The control-plane readiness board's **websites row** gains a **deploy-state drift** cell, and all three websites services expose an unauthenticated `/version` JSON, so the owner sees at a glance whether each live Railway service is running the latest merged code. **(1) Deployed-SHA source.** Each service reports the commit it is running, read **live from the environment** (no network): **`RAILWAY_GIT_COMMIT_SHA`** — the deployed commit Railway injects automatically — is the PRIMARY source; **`GIT_SHA`**, baked into each Dockerfile as a build arg (`ARG GIT_SHA=""` → `ENV GIT_SHA=$GIT_SHA`), is a local/non-Railway FALLBACK; neither set → the literal `"unknown"` (honest, never faked). The feature works with **zero Railway build-arg wiring** because the runtime primary is always present on Railway. **(2) `/version` endpoint** (`{"service", "sha", "short"}`) added to all three apps — `app/main.py` (control-plane; shape in `app/config.py` `deployed_sha()`/`version_info()`), `botsite/app.py`, `dashboard/app.py` (each a small self-contained `_version_info()`, since they are separate Railway services with separate build contexts) — **unauthenticated like `/healthz`** (a git sha is public data; no secret, no Railway account token). **(3) Board cell** (`app/readiness.py` `_service_deploy_state()` + `_deploy_board()`, rendered in `app/templates/board.html`): for each websites service it compares the DEPLOYED short-sha to the `main` HEAD short-sha the board already fetches live (`head_sha` from check-runs). control-plane reads its own deployed sha directly from env (no network hop for self); botsite/dashboard are fetched over their public `/version` through the existing TTL-cached raw client. The cell renders `in sync` (deployed == head), `DRIFT` (deployed ≠ head, both shown), or `unknown` (fetch failed / sha unset) — degrading per-service honestly, never crashing or faking. Only the websites row carries the cell (`deploy_state` is `None` for the other three repos, template-guarded); mobile-safe inside the existing `min-width:520px` scroll card. Tests +11 (all three suites: 84 → 95 passed): control-plane deploy-state in-sync/DRIFT/unknown-clean + `/version` env + unknown; botsite & dashboard `/version` primary/fallback/unknown. Local live-verify with a mock returning the real `main` head proved DRIFT (deployed `deadbeef` ≠ head `f2e2a1d5`) and in-sync (deployed == head) both render, `/version` returns the env sha.
- why: Railway auto-deploys each service from `main` on merge, so "is the live site on the latest code?" is a real, recurring operational question the board could not answer — a merged change is live "within minutes" but a *failed or stuck* deploy leaves the site stale silently. Surfacing DEPLOYED-vs-HEAD turns that into a glance: `in sync` = latest code live, `DRIFT` = a deploy in progress or stalled. Reading `RAILWAY_GIT_COMMIT_SHA` (the deployed commit Railway already injects) as the primary source means the signal is correct with no build wiring and no new dependency; the `GIT_SHA` Docker build-arg is a pure fallback for local runs. Keeping `/version` unauthenticated matches the public-surface posture (a commit sha is public), and reusing the board's existing live `head_sha` + TTL cache keeps it fast and drift-proof. All contained, reversible, forward-only, test-covered; **no `RAILWAY_API_KEY`/secret introduced, no ambient production `RAILWAY_*` IDs read (CI guard green), no destructive Railway op**.
- provenance: websites PR (order-001-deploy-drift session, 2026-07-09); manager ORDER 001 (control/inbox.md relayed via coordinator — the inbox/README contract files are not yet committed to the repo); docs/site.md § "Deploy-state drift + /version"; `.sessions/2026-07-09-order-001-deploy-drift.md`

## [D-0019] substrate-kit upgrade v1.0.0 → v1.2.0 via the §4.3 `upgrade` verb (+ .claude/ wiring, control fast lane)

- status: decided
- date: 2026-07-09
- verdict: The vendored `bootstrap.py` moves from kit **v1.0.0** to the released **v1.2.0** through the kit's own §4.3 consumer flow — the first *release-artifact* upgrade this repo has run (D-0017's bump was a re-vendor of dist HEAD). **(1) Verified**: the GitHub Release asset `bootstrap.py` (sha256 `258ab02aa54811d91b013f67a15d4bf13e8fc917421434746dd3ca26bc59098c`) matched `release.json` and `bootstrap.py.sha256`; `python3 bootstrap.py.new upgrade` self-verified before touching anything. **(2) Archive-first**: old dist banked to `.substrate/backup/bootstrap-1.0.0.py` + `state.json` backup — the committed `upgrade --rollback` path (same convention as superbot-next). **(3) Doc classification** (`.substrate/upgrade-report.md`): 15 planted docs `consumer-edited` (template unchanged — consumer-owned, nothing applied), 3 `diverged` — the `control/` trio, hand-planted from the superbot fleet spec (#25) before the kit shipped its own control templates, so no recorded hash; kept as-is (content matches the spec), manager may re-plant from the kit template. **(4) Staged artifacts regenerated**, including the new `.substrate/ci/substrate-gate.yml`. **(5) Engagement closed out**: `adopt --include-claude` planted `.claude/CLAUDE.md` (fully rendered from the filled slot context — zero `${...}`, no UNRENDERED banner) + `.claude/settings.json` (the four advisory fail-open kit hooks), and the last two interview slots were answered (`integration_mode=guided`, confirming the live mode; `doc_roots=docs`, graduating the derived value) — `ask` now reports no pending questions and the v1.2.0 engagement gate prints **ENGAGED**. **(6) Control fast lane** (KL-8) folded into the single required `quality` gate — a `control/**`-only diff (a status heartbeat) short-circuits the job GREEN **in-job** (never `paths-ignore`: a required context that never reports jams the merge), so heartbeats stop paying the heavy suite and need no session card. NO separate `substrate-gate.yml` workflow installed — same one-enforced-gate reasoning as D-0017; the kit's engagement checker explicitly accepts a hand-rolled gate ("a CI door exists", not "our exact file was copied"). **(7)** New v1.2.0 session-marker: cards now carry a `📊 Model:` line (config `session_markers` gained the needle in the upgrade). Session card records the before/after `check --strict` evidence; full app suite green.
- why: Staying a faithful kit consumer means taking releases through the kit's own upgrade contract (verify → archive → classify → apply-nothing-uninvited) rather than ad-hoc re-vendors, and v1.2.0's capabilities are ones this repo already needed: the status-freshness checker enforces the heartbeat discipline ORDER 001 started, and the fast lane keeps the required `quality` context from taxing (or jamming) coordination writes. Wiring `.claude/` closes the last "planted but inert" surface from the fleet review — future Claude sessions auto-load the rendered working agreement + advisory hooks instead of relying on orientation discipline alone. One enforced gate stays the rule; the staged substrate-gate.yml remains the kit-owned reference copy that upgrades keep fresh.
- provenance: websites PR #31 (kit-upgrade-engage session, 2026-07-09); substrate-kit Release v1.2.0 (https://github.com/menno420/substrate-kit/releases/tag/v1.2.0) + its `release.json`; `.substrate/upgrade-report.md`; `.sessions/2026-07-09-kit-upgrade-engage.md`

## [D-0020] Cross-repo activity timeline + idea-backlog views (self-directed)

- status: decided
- date: 2026-07-09
- verdict: The control-plane site gains two new **public** read-only views, built on the same TTL-cached `github` client the readiness board and journal browser already ride. **(1) `/activity` (+ `/activity.json`)** — one reverse-chronological stream that merges recent pull requests across **all four fleet repos** (superbot, superbot-next, substrate-kit, websites). `app/activity.py` fetches each repo's `/pulls?state=all&sort=updated` (bounded `PER_REPO_LIMIT=15`), normalizes each PR into `{repo, number, title, author, state, ts, url}`, collapses state to `merged | open | draft | closed`, sorts newest-first by merge/update time, and caps the combined stream at `TIMELINE_LIMIT=60`. `activity.html` badges each row by repo + state and deep-links to GitHub; a failing per-repo fetch degrades to an honest per-repo banner (never a silent drop), collected in `errors`. Decision-ledger entries were judged **not** cheap to merge in (per-repo ledger formats diverge — superbot has none, using the router) so v1 is PR-focused; a ledger lane can be added later. **(2) `/ideas` (+ `/ideas.json`)** — the `docs/ideas/` backlog per repo that keeps one. `app/ideas.py` lists each repo's ideas dir once (cache-backed contents API), excludes `README.md` (the index), sorts newest-first by date-suffixed filename, and enriches the newest `ENRICH_LIMIT=24` with a parsed title + one-line summary (`parse_idea`: strip YAML frontmatter → first `# H1` minus an `Idea:`/backtick label → a `**One line:**` marker or the first real paragraph, trimmed to one line ≤200 chars); the rest are surfaced as a count with a browse-all link so superbot's ~220-file set stays fast. A `404` on the ideas dir is an **absence** (`missing=True`), never an error banner; a non-404 listing failure surfaces `listing_error`. Each idea deep-links to GitHub **and** to the existing in-app markdown file view (`/journal/{repo}/file?path=…`), reusing the journal renderer rather than adding a second detail route. Nav links to both added in `base.html`; mobile-safe inside the existing `min-width:520px` scroll cards. Tests +9 (timeline merge/sort/links + honest error path + route-degrades; parse_idea frontmatter/One-line/Idea-label; ideas lists-real-files-skips-README + no-ideas-absence + listing-error + route-degrades). Parser validated against real fetched idea files (superbot + substrate-kit) via the raw host.
- why: With ten Projects running in parallel the owner wanted single glanceable screens for "what shipped / is in flight across the fleet" and "what's queued to build" — the claude.ai UI can't show cross-session activity, and the idea backlogs live scattered in each repo's `docs/ideas/`. Both are natural extensions of the control-plane's existing job (honest, cache-backed, per-cell-degrading views over live GitHub data), so they reuse the board's `github` layer with zero new dependency, zero new secret, and no Railway op. Reusing `/journal/{repo}/file` for idea detail keeps one markdown-render path. Contained, reversible, forward-only, test-covered; public-surface posture unchanged (all four repos are public; PR titles + idea files are public data).
- provenance: websites PR (activity-ideas-views session, 2026-07-09); self-directed improvement batch (orchestrator-relayed); `app/activity.py`, `app/ideas.py`, `app/templates/{activity,ideas}.html`, `app/main.py`, `app/templates/base.html`; docs/site.md § Routes; `.sessions/2026-07-09-activity-ideas-views.md`

## [D-0021] Fleet heartbeat page `/fleet` — every lane's `control/status*.md` on one screen (ORDER 002)

- status: decided
- date: 2026-07-09
- verdict: The control-plane site gains a new **public** read-only view **`/fleet` (+ `/fleet.json`)** that renders every fleet lane's `control/status*.md` heartbeat as one glanceable page — the owner's single control glance over the whole parallel fleet. `app/fleet.py` fetches each lane's status file over the shared TTL-cached `github.fetch_file` layer, parses the documented `control/README.md` format (`parse_status`: first `# <project> · status` heading → project name; documented `key: value` fields keyed by normalized name, where a NEW field only starts when the leading token is a **known key** so a colon inside a value — an ISO timestamp, a `#PR — text` line, a URL — never spuriously splits; a `⚑ needs-owner` leading flag is stripped; the optional substrate-kit `kit:` line is captured; unknown/wrapped lines append to the current field), classifies `health:` into `ok` (green) / `design` (red-by-design — purple, **never** counted broken) / `broken` (red) / `unknown`, computes heartbeat freshness with a **stale badge** past `FLEET_STALE_HOURS=12` (`freshness`: tolerant UTC ISO parse; an unparseable timestamp is honest `ok=False`, never faked fresh), attaches the repo's **last-commit age** + **open-PR count** (`repo_meta` over `/commits?per_page=1` + `/pulls?state=open`, each degrading to an honest "unknown"), and renders the full status body to HTML via the existing `journal.render_markdown`. `overview` fans out concurrently against a single `now`, sorts **attention-first** (fetch-error → broken → stale → unknown/absent → red-by-design → healthy; freshest within a rank) and rolls up summary counts (total / live / stale / broken / unreachable / no-file). Honest per-lane state: a `404` status file is an **absence** (`missing=True` — the bare `superbot` lane, whose heartbeat is written to superbot-next, is the real case), a non-404 fetch failure is an honest `fetch_error` banner. `fleet.html` shows a summary header + one card per lane (parsed field table + health/stale badges + repo meta + rendered body), each deep-linked to its `control/status*.md` on GitHub; `/fleet.json` drops the rendered body. Nav link added (placed after the board — the primary control glance); mobile-safe inside the existing scroll cards. Tests +8 (parse documented format incl. value-colons + `⚑`/`kit`; health kinds; freshness stale threshold + honest bad-timestamp; lane renders parsed health + body + repo meta; 404 = honest absence; non-404 = honest error banner; overview attention-sort + counts; route degrades no-auth incl. body-stripped JSON). Parser + render validated live against all 10 real lane status files via the raw host.
- lane set: the 10 lanes named in ORDER 002 and the manager's canonical registry `menno420/superbot` → `docs/eap/fleet-manifest.md` — superbot, superbot-next, substrate-kit, websites, trading-strategy, codetool-lab-fable5/opus4.8/sonnet5, and the two superbot-games cohabitation lanes (`status-mining.md` / `status-exploration.md`). Held as a hand-maintained `config.FLEET_LANES` list (the app-side copy of the manifest); **⚑ flagged to owner**: the canonical source is the manifest and this list must be kept in sync by hand until/unless `/fleet` parses the manifest live (a natural follow-up).
- why: ORDER 002 (fleet manager, P1): ten Projects now run in parallel and the owner tracks them by opening sessions one by one — the claude.ai UI can't show session activity, so the committed `control/status*.md` heartbeats are the only visible truth of a running agent. `/fleet` makes them one screen ("keep track of which agents are running and how far along they are"). A natural extension of the control-plane's existing job (honest, cache-backed, per-cell-degrading views over live GitHub data): reuses the board's `github` layer + the journal markdown renderer with **zero new dependency, zero new secret, no Railway op**. Contained, reversible, forward-only, test-covered; public-surface posture unchanged (every lane repo is public; heartbeat files are public data — the websites row dogfoods its own status).
- provenance: websites PR #35 (order-002-fleet-page session, 2026-07-09); fleet ORDER 002 (`control/inbox.md`, P1); `app/fleet.py`, `app/templates/fleet.html`, `app/config.py` (`FLEET_LANES` + `FLEET_STALE_HOURS`), `app/main.py`, `app/templates/base.html`; docs/site.md § Routes; `.sessions/2026-07-09-order-002-fleet-page.md`

## [D-0022] `/fleet` lane set derived LIVE from the fleet-manifest (self-directed; resolves the D-0021 drift ⚑)

- status: decided
- date: 2026-07-09
- verdict: `/fleet` now derives its lane SET **live** from the manager's canonical registry `menno420/superbot` → `docs/eap/fleet-manifest.md` instead of the hand-kept `config.FLEET_LANES` copy — closing the D-0021 ⚑ "keep it in sync by hand / drift risk" flagged to the owner. **(1) Live parse.** `fleet.resolve_lanes` fetches the manifest at request time over the same TTL-cached `github.fetch_file` layer the board already rides (superbot is **READ-ONLY** — fetched, never written). `fleet.parse_manifest` reads the manifest's markdown table, mapping columns by **header name** (Project / Repo(s) / Model / Notes …) so a column reorder can't shift a value into the wrong field, and extracts every `menno420/<repo>` reference from the Repo(s) cell. `fleet.manifest_to_lanes` expands rows into the existing lane-dict shape: the `manager` row (no concrete repo — control chair that builds nothing) is **skipped**; a **multi-repo** Project (the SuperBot coordinator, `superbot · superbot-next`) becomes **one lane per repo** (superbot's own `control/status.md` 404s as an honest absence since its heartbeat is written to superbot-next); a repo **shared** by >1 row (the two superbot-games cohabitation lanes) reads `control/status-<slug>.md`, slug derived from the Project name (`game-mining` → `mining`). Verified live against the real manifest: yields **exactly the 10 lanes**, byte-identical `(repo, status_path)` pairs to the prior hand-kept list — so the switch is drift-free by construction, and a lane **added to the manifest now auto-appears** with no code change. **(2) Honest fallback.** Any manifest fetch/parse failure (or a parse yielding zero lanes) falls back to `config.FLEET_LANES` (kept as the fallback) and the page shows a visible **"cached fallback list" banner** with the reason — never a silent pretend-live; `/fleet.json` exposes `lane_source` = `manifest` \| `fallback`. **(3) Unreadable ≠ dropped.** A manifest lane whose repo the token can't read (401/403) renders an honest `unreadable` state (underlying reason preserved) rather than being dropped. Tests +6 (manifest parse yields the expected 10 lanes matching the fallback set; a manifest-added lane auto-appears; resolve uses manifest when available; resolve falls back + labels on fetch failure; overview reports manifest-sourced; unreadable repo is honest not dropped). Full app suite green (117 passed).
- why: The lane set was the one hand-maintained copy on `/fleet` — D-0021 explicitly flagged it as a drift risk (a lane added to the manifest would silently not appear until someone hand-edited `config.FLEET_LANES`). Parsing the manifest live removes that class of drift entirely while keeping the hand-kept list as an honest fallback, so the page never goes blank if superbot is briefly unreachable. A natural extension of the control-plane's existing job (honest, cache-backed, per-cell-degrading views over live GitHub data): **zero new dependency, zero new secret, no Railway op**; superbot stays read-only. Contained, reversible, forward-only, test-covered.
- provenance: websites PR #36 (fleet-manifest-live session, 2026-07-09); self-directed improvement (resolves D-0021 ⚑); `app/fleet.py` (`parse_manifest`, `manifest_to_lanes`, `resolve_lanes`, `MANIFEST_REPO`/`MANIFEST_PATH`, `unreadable` lane state, `overview` lane_source), `app/templates/fleet.html`; docs/site.md § Routes; `.sessions/2026-07-09-fleet-manifest-live.md`

## [D-0023] Live-monitoring auto-refresh on `/fleet` + the board `/` (self-directed)

- status: decided
- date: 2026-07-09
- verdict: The two **live-monitoring** surfaces — the readiness board `/` and the `/fleet` heartbeat — gain an unobtrusive **client-side auto-refresh** so the owner's single control glance stays current without a manual reload; content/journal pages (`/journal*`, `/activity`, `/ideas`) stay static. **Mechanism (soft in-place refresh, chosen over a JSON re-render or a full-page reload):** `app/static/autorefresh.js` (a small vanilla-JS module, **zero new dependency**) re-fetches the CURRENT page over HTTP every `AUTOREFRESH_SECONDS` (config constant, default **45s**) and swaps only the server-rendered `#live-content` region into the DOM. This keeps the **server as the single renderer** — the refreshed content always matches a hard reload, with no duplicated Jinja logic in JS and no reload flash / scroll jump. The fetch omits `?refresh=1`, so it rides the 180s server TTL cache and never hammers the GitHub API on the owner's behalf. **Unobtrusive by contract:** a visible `auto-refreshing every Ns · last updated <time>` indicator (`_autorefresh.html`, included only on the two monitoring templates, sitting OUTSIDE `#live-content` so its control survives each swap) with a **pause/resume toggle** persisted in `localStorage`; polling also pauses while the tab is hidden and while a fetch is already in flight; a transient network error keeps the prior content and retries next tick; the pulse dot honors `prefers-reduced-motion`. Static assets are served by a new `StaticFiles` mount at `/static` (public, credential-free like the rest of the site; `app/static/` ships via the existing `COPY app ./app` Docker step). Tests +4 (board + fleet each carry the indicator markup + poll script + interval constant + pause control; content pages carry none; `/static/autorefresh.js` serves 200 as JS). Full app suite green (121 passed).
- why: With ten Projects running in parallel, `/fleet` and the board are *monitoring* screens the owner leaves open to watch the fleet — a static snapshot goes stale silently, so the owner had to remember to hit "refresh now". Auto-refresh makes them behave like the live dashboards they are. The soft `#live-content` swap was chosen over (a) a client-side JSON re-render — which would duplicate every server template's table logic in JS and drift — and (b) a `<meta http-equiv=refresh>` / `location.reload()` — which flashes and loses scroll position; the HTML-over-the-wire swap is the simplest approach that is both flash-free and always-correct. Scoped strictly to the two monitoring surfaces (an explicit ask: do NOT auto-refresh the whole site). A natural extension of the control-plane's existing job, **zero new dependency, zero new secret, no Railway op**; contained, reversible, forward-only, test-covered.
- also this session — quality.yml audit (no change): verified the required `quality` CI gate runs the FULL suite on every normal PR. A recent run finished in ~17s, which looked like a possible short-circuit; the run logs show it is genuinely just fast (install all three services' deps ≈7s + `bootstrap check --strict` + the ambient-Railway-ID guard + `pytest tests/ botsite/tests dashboard/tests` reporting `117 passed in 3.61s`). The only short-circuit in `quality.yml` is the **intentional, documented KL-8 control fast lane** (a `control/**`-only diff — a status heartbeat — short-circuits the required job green in-job, deliberately NOT a `paths-ignore` so a required context always reports). The prior ~17s FAILED run (29031306806) was the **born-red session gate** failing at the substrate-check step on an in-progress card, not a skipped suite. No change needed; confirmed with log evidence.
- provenance: websites PR #37 (monitoring-autorefresh session, 2026-07-09); self-directed improvement (orchestrator-relayed); `app/static/autorefresh.js`, `app/templates/_autorefresh.html`, `app/config.py` (`AUTOREFRESH_SECONDS`), `app/main.py` (`StaticFiles` mount + context), `app/templates/{base,board,fleet}.html`; docs/site.md §§ Routes / Live-monitoring auto-refresh / Env vars; `.sessions/2026-07-09-monitoring-autorefresh.md`

## [D-0024] Full project self-review + wake-up pass — gen-1 retro (ORDER 003), 18-card Model backfill (ORDER 004), project-review document

- status: decided
- date: 2026-07-09
- verdict: Owner-directed full project self-review + fleet wake-up pass. Read the inbox first; found ORDERS 003 (gen-1 retro) and 004 (Model-line backfill) still **genuinely unexecuted** — PRs #38/#39 were the *manager* appending those orders to `control/inbox.md` (commits authored by the owner), not the Project executing them; `status.md` still read `done=001,002`. Executed both. **(1) ORDER 003** — answered every `docs/retro/QUESTIONS.md` item by ID in `docs/retro/self-review-2026-07-09.md` (honest over flattering; each claim tied to a PR/commit/file; "cannot determine" used where true). **(2) ORDER 004** — backfilled the `📊 Model:` line on the 18 pre-kit-v1.2.0 `.sessions/` cards (value `claude-opus-4-8 (pre-v1.2.0 backfill; builder-session subagent, inherited — not independently confirmed)`), clearing the CI mtime-fallback landmine that reddened card-less PRs (e.g. PR #38 born-red on an unrelated old card); `bootstrap.py check --strict` now green. **(3)** Wrote `docs/retro/project-review-2026-07-09.md` — true current state (3 live services all in-sync at `6abe19f`, verified live), the full agent audit (every session/subagent, verified deliverables, honest model attribution, classified stalls), the honest efficiency verdict, the ⚑ owner-actions list, and the no-owner continuation. Docs-only session; no runtime/`app/` change. All three services verified live this session (`/healthz` 200, `/version` = `6abe19f` = `main` HEAD).
- why: The owner is designing gen-2 Projects from gen-1's lived experience — the friction *is* the deliverable (ORDER 003), and a gate tightened without a grandfather step left every future card-less PR born-red (ORDER 004). Executing both closes the inbox and turns the reconstructable record into a durable review. Contained, reversible, docs-only, forward-only; verified live.
- provenance: websites PR #40 (project-self-review session, 2026-07-09); fleet ORDERS 003 + 004 (`control/inbox.md`, P1); `docs/retro/self-review-2026-07-09.md`, `docs/retro/project-review-2026-07-09.md`, `.sessions/2026-07-09-*.md` (18 Model backfills), `.sessions/2026-07-09-project-self-review.md`, `docs/current-state.md`, `control/status.md`.

## [D-0025] `/activity.xml` — the cross-repo activity timeline as a subscribable Atom feed (self-directed)

- status: decided
- date: 2026-07-09
- verdict: The control-plane site gains a new **public** route **`/activity.xml`** that serves the SAME cross-repo PR activity timeline as `/activity` (+`/activity.json`) rendered as a valid **Atom 1.0 feed** (`Content-Type: application/atom+xml`), so the owner can *subscribe* to fleet PR activity in any reader (or pipe it to a Slack/Discord webhook) instead of polling the page. It is a **second serializer over the exact `activity.timeline()` list** — no new fetch path, no new data source, rides the same 180s TTL-cached `github.repo_api` layer the board/`/activity` already ride (zero new dependency, zero new secret, no Railway op). `app/activity.atom_feed(data, self_url, alternate_url)` builds the feed with **`xml.etree.ElementTree`** so every text node and attribute is XML-escaped by the library (never hand-concatenated): feed-level `title` "SuperBot fleet activity", `id` + `link rel=self` = the absolute `/activity.xml` URL (derived from `request.base_url`), a `link rel=alternate` to `/activity`, and `updated` = the newest entry's real timestamp. Each dated PR maps to an `<entry>`: `title` = `repo #num title`, `id` = the PR's GitHub URL (stable, globally unique), `updated` = its real merge/update time (`merged_at ‖ updated_at ‖ created_at` — the same honest `_timestamp_of` the HTML view uses), a `link` to the PR, `<author><name>`, and a short `summary` (repo · state · author). **Honest degradation:** a PR with no timestamp is **omitted** (never dated with an invented value); when the underlying fetch fails and nothing dated is available the feed **still validates** — it carries one diagnostic `<entry>` noting the empty/errored state stamped with the *clearly-derived* generation time, never a malformed feed or a fabricated PR. The `/activity` page advertises the feed with a `<link rel="alternate" type="application/atom+xml">` **discovery tag** in the head (new `{% block head %}` in `base.html`) and a visible **Subscribe (Atom)** link. Tests +4 (route → 200 + `application/atom+xml` + well-formed Atom parsed back with ElementTree, entries carrying real PR links/ids/timestamps + author; a title with `< & "` is escaped on the wire and round-trips to the original; offline degrades to a single-diagnostic-entry valid feed; `/activity` carries the discovery link + Subscribe link). Full app suite green (121 → 125 passed).
- why: `/activity` is pull-only — with ten Projects running in parallel a **push** surface (a feed a reader/webhook watches) is the natural next step: subscribe once and new merges arrive without visiting the site. An Atom feed is *just a second view* over a list the app already produces and caches, so the whole feature is one route + one serializer + a discovery link — no fetch, no dependency, no secret, no Railway op. Realizes the captured idea `docs/ideas/activity-atom-feed-2026-07-09.md`. Contained, reversible, forward-only, test-covered; public-surface posture unchanged (all repos are public; PR titles/links are public data).
- provenance: websites PR (activity-atom-feed session, 2026-07-09); self-directed improvement (realizes `docs/ideas/activity-atom-feed-2026-07-09.md`); `app/activity.py` (`atom_feed`, `_el`, `_summary_of`, `_now_rfc3339`, `ATOM_NS`), `app/main.py` (`/activity.xml` route), `app/templates/{base,activity}.html`; docs/site.md §§ Views/Routes; `.sessions/2026-07-09-activity-atom-feed.md`

## [D-0026] substrate-kit upgrade v1.2.0 → v1.6.0 via the §4.3 `upgrade` verb (capabilities ledger, owner-action format, claim ritual, kit heartbeat line)

- status: decided
- date: 2026-07-09
- verdict: The vendored `bootstrap.py` moves from kit **v1.2.0** (D-0019, PR #31) to the released **v1.6.0** through the kit's own §4.3 consumer flow. **(1) Verified**: the Release asset (sha256 `787d561728f64070efd7e25db05a1264db24b4bee66b08a296ebd205d6d8060f`) matched `release.json` and `bootstrap.py.sha256` before running (checked by hand with `sha256sum`; note — `bootstrap.py.new upgrade` skipped its own sha check because `release.json` wasn't copied next to it: place it there next time to get the in-flow verification too). **(2) Archive-first**: old dist banked at `.substrate/backup/bootstrap-1.2.0.py`, `last-upgrade.json` records from=1.2.0 → to=1.6.0 with the committed `upgrade --rollback` path; the new dist is banked alongside as `bootstrap-1.6.0.py`; the input `bootstrap.py.new` self-cleaned. **(3) Report classification** (`.substrate/upgrade-report.md`): 13 planted docs `consumer-edited` (kept, nothing to apply), **5 `diverged`** — CONSTITUTION.md, docs/collaboration-model.md, docs/AGENT_ORIENTATION.md, control/README.md, control/status.md — kept live, with the report's **additive template deltas hand-merged** (capabilities-discovery bullet + owner-attention rail into CONSTITUTION; "Routing work to the owner" into collaboration-model; CAPABILITIES.md into the orientation reading order + planted-set list; multi-lane heartbeats + claim-FIRST ritual + `kit:` line + OWNER-ACTION format into control/README.md; status.md gets the `kit:` line in its session-close overwrite), **1 `template-improved` applied** (`.claude/CLAUDE.md`, consumer-untouched — staged copy taken verbatim, still fully rendered, zero `${...}`), **1 `missing` replanted** (`docs/CAPABILITIES.md` — new v1.5.0 doc, planted with the fleet's lived seed walls and **seeded same-session with this repo's verified findings**: release-asset HTTPS downloads work despite the api.github.com wall; branch-push + MCP create/merge work here; `/version`-vs-main-HEAD live deploy verification; the control-plane GITHUB_TOKEN degraded-cells wall). **(4) Post-upgrade gate**: `bootstrap.py --version` → 1.6.0; `check --strict` had 1 enforced finding (CAPABILITIES.md orphan — fixed by the orientation wiring, the template's own intent) + the new **owner-action advisory** on `control/status.md` (fixed honestly: the two genuinely actionable asks reformatted as full six-field OWNER-ACTION blocks with real VERIFIED-NEEDED walls; the five decision forks stay parked in `docs/owner/OWNER-ACTIONS.md` per fewer-clearer-asks); now green (only the intended born-red card hold until flip). **(5)** `kit_version` config + the D-0019 exact-pin test both moved to 1.6.0; full suite **125 passed**; ambient-Railway-ID guard OK. **(6)** New v1.4.0 config key `heartbeat_files: ["control/status.md"]` recorded (single-lane default, declared explicitly). Status heartbeat now carries the v1.3.0 `kit: v1.6.0 · check: green · engaged: yes` self-report line.
- why: Staying a faithful kit consumer means taking releases through the kit's upgrade contract (verify → archive → classify → apply-nothing-uninvited), and the v1.3.0–v1.6.0 bands are conventions this repo already lives by or needed: the kit heartbeat line is how the substrate coordinator sees this lane, the capabilities ledger makes session walls durable instead of re-discovered, the six-field owner-action format is the same quality bar ORDER 005 already references, and the claim-FIRST ritual is the root-cause fix for the twin-execution failure this fleet actually suffered (kit PRs #50/#51). Hand-merging the additive deltas into the diverged docs keeps consumer-owned content while inheriting the doctrine — the report's diffs made that a mechanical, verifiable merge.
- provenance: websites PR #45 (kit-upgrade-v1.6.0 session, 2026-07-09); substrate-kit Release v1.6.0 (https://github.com/menno420/substrate-kit/releases/tag/v1.6.0) + its `release.json`; `.substrate/upgrade-report.md`; `.substrate/backup/last-upgrade.json`; `.sessions/2026-07-09-kit-upgrade-v1.6.0.md`; PING-ACK: PR #44.

## [D-0027] `/queue` owner to-do surface + `/environments` registry view (ORDER 005), with honest degradation for the runtime-only fleet-manager source

- status: decided
- date: 2026-07-10
- verdict: The control-plane site gains the two ORDER 005 **public** read-only pages. **(1) `/queue`** — the owner's single to-do surface. Lane half: every fleet lane's `⚑ needs-owner` field, **reusing `fleet.overview()`** (the exact /fleet pipeline — same fetch, same parse, same TTL cache; never an independent refetch/reparse). Manager half: `menno420/fleet-manager docs/owner-queue.md` via `github.fetch_file`. `app/owner_queue.py` parses the fleet's six-field OWNER-ACTION format (WHAT/WHERE/HOW/WHY-IT-MATTERS/UNBLOCKS/VERIFIED-NEEDED) with one tolerant parser covering BOTH shapes the data actually arrives in — the flattened one-line string `fleet.parse_status` produces for lane fields, and raw multiline markdown (with optional `**label:**` emphasis) from the manager's doc. Structured blocks render as cards (WHAT = headline, remaining fields a table); a plain ask with no blocks is one honest free-text item; a lane's pre-block pointer sentence becomes a small "lane note", not an ask. **Deduplication**: same normalized WHAT/text merges into ONE item that keeps *every* source badge (lane name + heartbeat age + deep link). **Ordering**: newest-first by source-lane heartbeat timestamp; undated items (the fleet-manager doc carries no timestamp) sort last labeled "date unknown" — never given an invented time. A fetched owner-queue.md containing NO structured blocks yields **zero** list items (flattening a whole document into one giant "ask" is noise, not an item — caught live during local verification) and instead renders in full below, labeled. **(2) `/environments`** — read-only render of the fleet's claude.ai environment registry `fleet-manager environments/` (README first, then root files, then one subdirectory level, bounded `MAX_FILES=40`, TTL-cached contents-API listing): markdown via the sanitized `journal.render_markdown`, setup scripts / env-var schemas as escaped code blocks with a **copy-to-clipboard** button (`app/static/copycode.js`, vanilla JS, no dependency, silent no-op without a secure-context Clipboard API). The manager's repo stores, the site renders; secrets never present by design (registry stores names/placeholders only). **(3) Honest degradation on both** (the dispatch-declared constraint): fleet-manager is readable only at runtime — `GITHUB_TOKEN` unset **and** the fetch failed → a clear **not configured** banner naming the filed OWNER-ACTIONS ask; token set but fetch failed → **unavailable** with the reason; unreadable lane heartbeats → an "asks may be missing" count banner; per-file/per-subdir errors degrade per-cell; both routes always 200, never fabricate. Live-local verification found the raw-host path can read `docs/owner-queue.md` anonymously (the manager half went live, not degraded, in the token-less container exercise), so the pages key their degraded states off the actual fetch result, not an assumption about the token. Nav links added (after `/fleet`); neither page auto-refreshes (D-0023 scope kept). Tests +15 (125 → 140 across the three suites): parser (flattened / multiline / none / free-text), dedup + newest-first + sources-kept, not-configured vs unavailable on both modules, no-blocks-not-flattened, routes degraded (token unset, upstream down → 200 + honest banners) and happy (mocked upstream → structured fields, README-first render, copy JS served), D-0023 no-autorefresh scope. Also ORDER 007 step 4: `scripts/env-setup.sh` committed as a wrapper `exec`-ing the tested `scripts/setup-env.sh` (the pinned-research archetype expects that exact path; same always-exit-0, never-print-secrets contract; smoke-run verified exit 0).
- why: ORDER 005 (P1): the owner asked for one place to see everything he needs to do, and an environment builder surface — with ten lanes running in parallel the ⚑ asks live scattered across ten heartbeat files plus the manager's curated doc. Both pages are natural extensions of the control-plane's existing job (honest, cache-backed, per-cell-degrading views over live GitHub data): zero new dependency, zero new secret, no Railway op; fleet-manager stays read-only. Reusing `fleet.overview()` keeps ONE fetch+parse path for heartbeat data (no drift between /fleet and /queue). Contained, reversible, forward-only, test-covered; public-surface posture unchanged (heartbeats are public data; the registry is secrets-free by design).
- provenance: websites PR #53 (order-005-queue-environments session, 2026-07-10); fleet ORDER 005 + ORDER 007 steps 3–5 (`control/inbox.md`); claim PR #52 (`claimed-by: 005 gen2-order-005 2026-07-10T02:24Z`); `app/owner_queue.py`, `app/environments.py`, `app/templates/{queue,environments}.html`, `app/static/copycode.js`, `app/main.py`, `app/templates/base.html`, `scripts/env-setup.sh`; docs/site.md §§ 3b/3c/Routes/Env vars; `.sessions/2026-07-10-order-005-queue-environments.md`

## [D-0028] Enriched machine-readable heartbeat fields on `/fleet` — parsed `orders:`, optional `routine:` / `landing:` / `deployed:` lines (queue-state NEXT item 4, retro G3)

- status: decided
- date: 2026-07-10
- verdict: The `/fleet` heartbeat parser (`app/fleet.py`) now machine-reads the status format instead of only displaying it. **(1) `orders:` parsed** (`parse_orders`): `acked=`/`done=` id lists with range expansion (`001-008`; zero-padding kept, a >500-wide range is refused as a typo, duplicates dropped), **outstanding = acked minus done** — the ids a lane has SEEN but not finished, computable from the heartbeat alone (no inbox diff needed); a `claimed-by:` annotation (order-claim ritual) is captured verbatim; a free-text orders line parses honestly to `ok=False`, never invented ids. **(2) Three OPTIONAL status lines** added to the documented format (`control/README.md`) and `KNOWN_KEYS` (so they stop leaking into `blockers:` as line-continuations — the websites heartbeat's own `routine:` line was doing exactly that on the live page): `routine:` (`classify_routine` — armed flag, cron, last-fired age; **silent** = armed with a last fire older than `FLEET_STALE_HOURS` — the "armed but silently dead" wake clock the field exists to catch; armed with NO parseable fire = honest `no_fire_recorded`, not silent); `landing:` (`classify_landing` — `clean` / `pushed` (pushed-unmerged, a PR-less rescue candidate) / `local` (LOCAL-ONLY — the 2026-07-10 16:01Z stranded-work incident class) / `unknown`, with the `claude/*` branch extracted; pushed/local set `attention`); `deployed:` (last live-verified sha, passed through as written — no classification to get wrong). **(3) Surfacing**: lanes with a stranded landing or silent routine **sort attention-first** (join the stale rank); per-lane rows badge outstanding ids / "armed but no fire in Xh" / "LOCAL-ONLY — stranded work" / "pushed, unmerged — rescue candidate"; the summary header rolls up `stranded` / `silent_routines` / `outstanding_orders`; `/fleet.json` carries the parsed `orders_info` / `routine_info` / `landing_info` structures for the manager. **(4) Backwards-honest**: a lane writing none of the optional lines renders exactly as before (all parses tolerate absence; `present=False`, zero attention flags, no invented values). Tests +14 (143 → 157 across the three suites): range/comma/claimed/free-text/absurd-range orders parses; routine armed-fresh / armed-silent / armed-no-fire / prose-form / none; landing kinds + attention + branch extraction; lane_status flow (enriched fields through, no blockers leak) and plain-lane unchanged; overview sort (stranded green lane above plain healthy) + roll-up counts. Live-local render verified: `/fleet` HTML shows every new badge, `/fleet.json` the structures.
- why: Queue-state NEXT item 4 (retro G3): the manager computed "what's left" by diffing inbox vs status vs git by hand. The heartbeat already carries the data — parsing it makes `/fleet` the single glance for outstanding work, silently-dead wake clocks (a lane that armed a routine and then never fired is invisible today), and stranded branches (the 16:01Z incident class gets a standing mechanical catch). Folds in the two sibling backlog captures (`routine:` and `landing:` lines) as designed in `docs/ideas/backlog.md`. Contained, reversible, zero new dependency/secret/Railway op; protocol change is additive-optional so no other lane must change anything.
- provenance: websites PR #67 (heartbeat-enrichment session, 2026-07-10, continuous-mode slice 2); queue-state NEXT item 4 + `docs/ideas/backlog.md` planned bullet (sources: retro G3, `.sessions/2026-07-10-gen2-closeout-docs.md` 💡, `.sessions/2026-07-10-rescue-landing-project-package.md` 💡); `app/fleet.py` (`parse_orders`, `_expand_ids`, `classify_routine`, `classify_landing`, `KNOWN_KEYS`, `_sort_key`, `overview` summary), `app/templates/fleet.html`, `control/README.md` (format block + enrichment note), `tests/test_fleet_enrichment.py`; docs/site.md § 3a; `.sessions/2026-07-10-heartbeat-enrichment.md`

## [D-0029] Scheduled healthcheck workflow — standing liveness verification on the repo's own clock (backlog promotion)

- status: decided
- date: 2026-07-10
- verdict: New **non-required** GitHub Actions workflow `.github/workflows/healthcheck.yml`: cron every 6 hours (minute-17 offset — GitHub documents top-of-hour cron congestion can delay/drop runs) plus `workflow_dispatch`, running the repo's existing `scripts/healthcheck.py` — the three live Railway services must answer `/healthz` and `/` with 200, and the LIVE fleet-manifest must still parse to a non-empty lane set through the SAME `app.fleet` parser `/fleet` uses (the retro-A3 silent-fallback alert). **Read-only by design**: probes public endpoints + one public raw file; zero secrets, zero Railway credentials, zero writes. Deliberately NOT a required status check — it gates nothing; a failure notifies via the failed-workflow email the owner already receives and shows red on the Actions tab. First run verified via `workflow_dispatch` after merge (recorded on the session card).
- why: The gen-1 final retro handed over with "liveness unverified at handover — a successor should run healthcheck.py before trusting liveness", and retro F3 named Actions cron as the one scheduler agents can arm themselves. A 6-hourly scheduled probe turns liveness (and manifest-parse health) from session memory into repo infrastructure — the same silent-degradation class the 4-hourly wake routine covers for work, covered for the deployment. Contained, reversible (delete one file), no new dependency beyond the app's own requirements.
- provenance: websites PR #69 (scheduled-healthcheck session, 2026-07-10, continuous-mode slice 3); promoted from `docs/ideas/scheduled-healthcheck-workflow-2026-07-10.md` (gen-2 night-prep seed, origin consumer:menno420/superbot); `.github/workflows/healthcheck.yml`; `scripts/healthcheck.py` unchanged (D-0015 + the PR #59 manifest check); `.sessions/2026-07-10-scheduled-healthcheck.md`

## [D-0030] `/projects` — the fleet-manager Project-package registry rendered read-only (ORDER 009 increment 1)

- status: decided
- date: 2026-07-10
- verdict: New public control-plane route **`/projects`** (+ `/projects.json`) rendering `menno420/fleet-manager` → `projects/` — the fleet's Project-package registry, one card per `projects/<repo>/` package. Per card: the package's files classified by tolerant basename heuristics into the roles a Project runs on (meta / Custom Instructions / coordinator prompt / setup script / failsafe / wake-routine prompt; anything unrecognized listed honestly as "other"), each deep-linked to GitHub; `meta.md` rendered inline via the sanitized `journal.render_markdown`; a best-effort deployed-state badge from the first `deployed:`/`state:`/`status:`-keyed line of `meta.md` (markdown emphasis tolerated; no such line → "state unknown", never invented). Bounded registry walk (`MAX_PACKAGES=30`, `MAX_FILES_PER_PACKAGE=20`) over the SAME TTL-cached `github` layer as `/fleet`/`/queue`/`/environments`; fleet-manager stays READ-ONLY. **Degradation extends the ORDER 005 model with an `empty` state** — the dispatch declared the upstream `projects/` folder "being created RIGHT NOW", and this session verified it (raw `projects/README.md` → 404 while the repo root README is 200 anonymously), so a 404 on the folder renders a friendly "registry not landed yet" banner (an absence, not an error) and fills in the moment the folder lands; non-404 failures split not-configured (GITHUB_TOKEN unset) / unavailable (reason surfaced) exactly like `/queue`; per-package listing and per-meta fetch failures degrade per-card. Route always 200. Nav link added after `/environments`; `/projects.json` drops the rendered meta HTML (the `/fleet.json` precedent). Tests +8 (157 → 165): role heuristics, tolerant state extraction + honest absence, the full degradation ladder (empty-on-404, dir-exists-but-empty, not-configured vs unavailable), happy-path package cards (role-first ordering, meta render, state badge), route smoke (200 + banner + nav link) and JSON shape (meta_html stripped).
- why: ORDER 009 (P1, owner directive ~20:50Z): all fleet/project information centralizes in fleet-manager AND must be browsable on the website — the site renders, the manager's repo stores. The `projects/` registry is the fleet's operational package store (what each Project actually runs on), and it was browsable nowhere. Shipping increment (1) immediately — with the empty state as a first-class citizen — means the page is live BEFORE the registry finishes landing, so the owner watches it fill in rather than waiting for a second deploy. Contained, reversible, zero new dependency/secret/Railway op, public-surface posture unchanged (the registry is secrets-free by design, names/placeholders only).
- provenance: websites PR #72 (order-009-projects session, 2026-07-10, continuous-mode slice 4); fleet ORDER 009 (`control/inbox.md`, appended PR #70); claim PR #71 (`claimed-by: 009 websites-continuous-wake 2026-07-10T21:07Z`); `app/projects.py`, `app/templates/projects.html`, `app/main.py` (routes), `app/templates/base.html` (nav), `tests/test_projects.py`; docs/site.md §§ 3d/Routes; `.sessions/2026-07-10-order-009-projects.md`

## [D-0031] `/reviews` — the fleet's post-merge review-queue ledger rendered with extracted findings links (ORDER 009 increment 3)

- status: decided
- date: 2026-07-10
- verdict: New public control-plane route **`/reviews`** (+ `/reviews.json`) rendering `menno420/fleet-manager` → `docs/review-queue.md`, the fleet's post-merge second-review ledger (gen-2 merge-authority: no PR waits for pre-merge review; flagged PRs merge and get a ROW; veto = revert). Placement was the implementer's call in the order (/fleet, /queue, or a docs panel) — a dedicated small page keeps `/fleet`'s fetch fan-out unchanged and the review queue is quality debt, not an owner to-do. `app/reviews.py` parses the ledger's markdown table by HEADER NAME (column reorder can't shift a value — the fleet-manifest parser's lesson): each row → `{repo#N (deep-linked to the real PR), what-to-re-check, why-risky, drain-path/status}`; struck `~~…~~` rows classify as REVIEWED and list separately; open/reviewed roll-up badges; the FULL doc renders below via the sanitized `journal.render_markdown` so nothing is hidden by the parse; a doc with no tables yields zero rows, never an invented one. **Findings links are extracted from the ledger itself** — every markdown link into the manager's `findings/`/`planning/` trees (the night-review, fleet-economics, launch-record docs the rows cite) resolved relative to `docs/`, deduplicated, surfaced as a "findings & records" list: extraction instead of hardcoding means an upstream rename never leaves the page pointing at a ghost. Honest degradation ladder as `/projects` (D-0030): empty on 404 / not-configured (token unset) / unavailable (reason surfaced); always 200; `/reviews.json` drops the rendered HTML. Same TTL-cached `github` layer; fleet-manager READ-ONLY; zero new dependency/secret. Verified against the LIVE upstream doc pre-merge: 8 rows parsed (all open), all 3 findings links extracted and resolving 200. Tests +8 (165 → 173): header-name parsing + PR deep-links, struck-row classification, no-table honesty, link extraction/resolution/dedup, the state ladder, happy-path counts + full render, route smoke (200 + nav + JSON shape), empty-state 200.
- why: ORDER 009's audit named `docs/review-queue.md` rows and the launch-readiness/economics findings docs as browsable nowhere — and the review queue is the safety valve the whole no-pre-merge-review policy rests on (its own binding header says so), so invisibility is a policy risk, not a cosmetic gap. Increment (3) was ledgered as backlog at increment-(1) ship time per the order's done-when; this closes it for real one slice later. Contained, reversible, public-surface posture unchanged (the ledger is public data).
- provenance: websites PR #75 (order-009-reviews session, 2026-07-10, continuous-mode slice 5, 21:40Z send_later continuation); fleet ORDER 009 increment (3) (`control/inbox.md`; order claimed via PR #71, increments 1-2 closed via PR #72); `app/reviews.py`, `app/templates/reviews.html`, `app/main.py` (routes), `app/templates/base.html` (nav), `tests/test_reviews.py`; docs/site.md §§ 3e/Routes; `.sessions/2026-07-10-order-009-reviews.md`
