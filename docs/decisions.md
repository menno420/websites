# websites — decisions

> **Status:** `living-ledger`
>
> Generated by substrate-kit. Append-only decision ledger — entries are
> superseded, never deleted. Rule docs cite entries as bare [D-NNNN] ids;
> this file holds the provenance so rules never narrate it inline.

<!-- Grammar: ## [D-NNNN] <title> / - status: decided|superseded|retired / - date: YYYY-MM-DD / - supersedes: D-NNNN (opt) / - superseded-by: D-NNNN (opt) / - verdict: <one line> / - why: <2-3 lines> / - provenance: <ref> -->

## [D-0001] Adopt the substrate-kit workflow

- status: decided
- date: 2026-07-09
- verdict: websites runs on the substrate-kit agent workflow.
- why: A repo-resident working agreement, decision ledger, and session
  discipline let agents work correctly with little steering; adopting the
  kit starts websites governed instead of accreting rules ad hoc.
- provenance: substrate-kit adoption interview

## [D-0002] Adoption provenance pin
- status: decided
- date: 2026-07-09
- verdict: Adopted fresh-from-kit: menno420/substrate-kit main @ 08a291f96599d5b7ec1b6a46a381d4e3f88839c0, dist/bootstrap.py blob 7d2b85c740b952aa8c52d013fdcfa182f395b509 (643694 bytes, stdlib-only), command: python3 bootstrap.py adopt.
- why: Pin exactly which kit build planted this repo so later upgrades diff against a known source; mirrors superbot-next's D-0002. Never a copy of superbot.
- provenance: superbot docs/planning/websites-project-kickoff-2026-07-09.md (PR #1876), sequence step 1

## [D-0003] Control-plane site stack: FastAPI + Jinja2 + httpx, server-rendered, no build step
- status: decided
- date: 2026-07-09
- verdict: The control-plane site is a single server-rendered FastAPI app (Python 3.12, Jinja2 templates, httpx client, Markdown renderer), pinned deps, simple Dockerfile binding 0.0.0.0:$PORT.
- why: Boring and Railway-friendly; matches the inherited taste from superbot's dashboard/ (small FastAPI apps, minimal deps, honest data); no frontend build step to break. Async httpx lets one page fan out ~30 GitHub calls concurrently.
- provenance: websites PR #2 (control-plane-site session, 2026-07-09); kickoff brief 'stack: builder's free choice — decide and flag'

## [D-0004] Control-plane data/auth/cache model
- status: decided
- date: 2026-07-09
- verdict: All GitHub data fetched live server-side: REST API for listings/state (GITHUB_TOKEN), raw.githubusercontent.com WITHOUT auth for public file bodies; per-URL in-memory TTL cache 180s caching successes and stable negatives (404/403/401) but never transient errors; ?refresh=1 busts. Auth = HTTP Basic, any username, password compared constant-time to SITE_PASSWORD (fail closed if unset); /healthz unauthenticated. Every fetch degrades per-cell — the board renders 'unknown (reason)' instead of faking or 500ing (e.g. secrets without admin scope, rulesets without scope).
- why: 3-min TTL keeps a board load ~30 API calls well under PAT rate limits while staying near-live; raw host needs no token for public repos but 404s if sent a bad bearer (found live); shared-secret Basic auth is exactly the 'something simple' the kickoff asked for.
- provenance: websites PR #2 (control-plane-site session, 2026-07-09)

## [D-0005] Deployment target: dedicated Railway project `superbot-websites`
- status: decided
- date: 2026-07-09
- verdict: The site deploys as service `control-plane` in its own fresh Railway project `superbot-websites` (IDs in docs/deployment.md), repo-connected to menno420/websites@main — merge to main IS the deploy. Never deployed into, or configured via, the production bot project.
- why: Hard isolation from the live bot (the ambient Railway IDs in agent containers point at production — a standing footgun); repo-connect makes redeploys zero-step and keeps the forward-only-git flow the sole change path.
- provenance: websites PR #3 (railway-deploy session, 2026-07-09); kickoff brief Phase 3

## [D-0006] Durable owner PAT wired as the control-plane service token — board fully live
- status: decided
- date: 2026-07-09
- verdict: A durable owner GitHub PAT is set as the `control-plane` service token in the `superbot-websites` Railway project (variable upsert via the public GraphQL API, RAILWAY_API_KEY only, ambient production-bot IDs never passed, no destructive mutation). The board now runs on the authenticated GitHub rate limit; the auth-gated cells (actions secret names, auto-merge, CODEOWNERS presence) render live and were verified on the deployed site. Per-cell degradation still stands for anything the token scope or egress can't reach — the board never fakes a value.
- why: Closes the one outstanding owner TODO from the deploy session; lifts the board off the unauthenticated rate limit and unlocks the secrets / auto-merge / CODEOWNERS cells so the oversight board actually shows the config state it exists to surface.
- provenance: websites PR #6 (token-wiring session, 2026-07-09); supersedes the owner-TODO left open by [D-0005]

## [D-0007] Rework plan for dashboard/ + botsite/ drafted, awaiting owner review
- status: decided
- date: 2026-07-09
- verdict: The step-3 rework of superbot's dashboard/ and botsite/ into this repo is planned in docs/planning/dashboard-botsite-rework-plan-2026-07-09.md — plan only, no code ported, live sites untouched. Recommended shape: keep ideas/functionality, rebuild implementation on this repo's substrate; ONE repo with MULTIPLE Railway services (not one shared FastAPI process) so the public botsite stays secret-isolated from the private control-plane/dashboard; botsite reworked first, dashboard second; consume superbot's committed JSON artifacts via raw GitHub (read-only, forward-only). Seven open questions routed to the owner (public/private per site, exact-design-vs-restyle, merge-vs-separate, control-panel coupling to the live bot, submissions pipeline, domains, data feed).
- why: The kickoff makes step 3 plan-first — describe the plan before starting the rework — because it touches tools the owner uses today. This entry records that the plan exists and is the gate; implementation waits on owner review of the open questions (esp. Q4, the control panel's coupling to the running production bot). Numbered D-0007 (not D-0006) because a concurrent token-wiring session claimed D-0006 in flight the same day.
- provenance: websites PR #4 (dashboard-botsite-rework-plan session, 2026-07-09); superbot docs/planning/websites-project-kickoff-2026-07-09.md sequence step 3

## [D-0008] Botsite rebuilt + deployed as a second Railway service (public, server-rendered, ds/-styled)
- status: decided
- date: 2026-07-09
- verdict: The public bot site is rebuilt on this repo's substrate under `botsite/` (server-rendered FastAPI + Jinja2, no build step) and deployed as a NEW Railway service `botsite` (ID 4314f839-0a93-4995-b424-02861ad2d5e6) in the existing `superbot-websites` project, alongside `control-plane`, dark-launched at https://botsite-production-cfd7.up.railway.app (Root Directory = `botsite`, own Dockerfile, repo-connected to main = merge deploys). It reproduces superbot botsite's public surface (Home/Features/Features-detail/Commands/Games/Changelog/Status/Design/Submit + /healthz + /palette.json), styled with the promoted `ds/` design system (tokens.css + components.css verbatim, plus ds.js). Data is superbot's committed `site.json` consumed live via raw.githubusercontent.com with a 180s TTL cache — read-only + forward-only toward superbot, never faking data (a failed feed shows an honest banner). The v1/v2 SPA duality and Tailwind-CDN are dropped for one server-rendered site on one design system. `/submit`'s DB write path is a clearly-labeled stub until the submissions Postgres is provisioned.
- why: Executes rework-plan step 3 (botsite half, [D-0007]) with the plan's recommended shape: keep ideas/functionality, rebuild the implementation; one repo, multiple Railway services so the public marketing surface stays secret-isolated from the private control-plane; consume the committed JSON artifact rather than rebuilding the export tooling here (plan Q7). Public/server-rendered/ds-styled resolves plan Q1/Q2 with the reasonable defaults; submissions (Q5) and custom domains (Q6) stay deferred to the owner. Created with RAILWAY_API_KEY + explicit superbot-websites IDs only; ambient production-bot IDs never passed; no destructive Railway mutation.
- provenance: websites PR #7 (rework-botsite session, 2026-07-09); docs/botsite.md; docs/planning/dashboard-botsite-rework-plan-2026-07-09.md

## [D-0009] Dashboard rebuilt + deployed as a third Railway service (private, read-only oversight; live-bot control panel stubbed by design)
- status: decided
- date: 2026-07-09
- verdict: The developer dashboard is rebuilt on this repo's substrate under `dashboard/` (server-rendered FastAPI + Jinja2, no build step) and deployed as a NEW Railway service `dashboard` (ID 39007299-11a2-49a8-9c5c-21e17194fb3e) in the existing `superbot-websites` project, alongside `control-plane` and `botsite`, dark-launched at https://dashboard-production-a91b.up.railway.app (Root Directory = `dashboard`, own Dockerfile, repo-connected to main = merge deploys). It is PRIVATE: every route is gated behind HTTP Basic auth (any username + SITE_PASSWORD, constant-time; fail-closed 503 if unset), same pattern as control-plane; /healthz stays unauthenticated for Railway. The read-only oversight surface is fully live — Overview / Functions / Commands / Aliases (client-side collision-checker) / Settings / Access / Env-map (names + locations only, never values) / Ideas / Bugs / Updates / Console / Status + /palette.json — reproducing superbot dashboard's read-only pages, styled with the vendored `ds/` design system. Data is superbot's committed `dashboard.json` + `console.json` consumed live via raw.githubusercontent.com with a 180s TTL cache — read-only + forward-only toward superbot, never faking data (a failed feed shows an honest banner). THE ONE DELIBERATE STUB: superbot's Discord-OAuth control panel writes the LIVE PRODUCTION BOT's control API (settings/help/cog-routing applied live) plus an owner-gated submissions-moderation ring; that live-write path couples websites to the running production bot across the repo boundary and is owner-decision Q4, so it is NOT wired here. `/admin` renders the UI shape but every mutating action is a disabled placeholder ("requires owner wiring — not connected to the live bot"), and NO production bot control-API URL or token exists anywhere in the service (a test asserts the literal names never appear).
- why: Executes rework-plan step 3 (dashboard half, [D-0007]) with the plan's recommended shape: keep the read-only ideas/functionality, rebuild the implementation; one repo, multiple Railway services; consume the committed JSON artifacts rather than rebuilding the export tooling here (plan Q7). Private-Basic-auth resolves plan Q1 (the oversight data is internal, so gate it like control-plane). The live-bot-write control panel stays a labeled stub because carrying it would wire websites to the running production bot — an owner call (Q4) that must not be made implicitly; the safe, reversible default is to ship the read-only surface live and stub the mutating path with zero production credentials present. Created with RAILWAY_API_KEY + explicit superbot-websites IDs only; ambient production-bot IDs never passed; no destructive Railway mutation. The live superbot dashboard keeps running unchanged.
- provenance: websites PR #8 (rework-dashboard session, 2026-07-09); docs/dashboard.md; docs/planning/dashboard-botsite-rework-plan-2026-07-09.md

## [D-0010] console.json consumed under a pinned, versioned cross-repo shape contract
- status: decided
- date: 2026-07-09
- verdict: The `/console` page's feed (superbot's committed `botsite/data/console.json`) is no longer consumed on an implicit schema. Superbot commits the canonical versioned contract (`botsite/data/console_data_contract.json`, superbot PR #1884: top-level families + guaranteed fields per record + `version`), stamps `meta.schema_version` into every emitted feed, and enforces producer-side parity + fail-closed shape checks in its CI. This repo pins the contract copy it was built against (`dashboard/console_data_contract.json`, v1) and verifies at render time (`data_source.console_contract_issue`): version mismatch or a missing contracted family renders an honest schema-drift banner on `/console` (never-fake-data posture) instead of a silently wrong page. Shape changes flow as: superbot edits the canonical contract + bumps version (explicit, reviewable) → this repo syncs the pinned copy + adapts templates/fixture in one commit.
- why: The two repos shared an implicit schema — a producer-side family/field rename would silently blank or corrupt the consumer page (superbot's BUG-0022 desync class) with no signal on either side. The first pass of writing the consumer against the real contract immediately proved the point: the console page treated `ideas`/`bugs` as lists (`|length`) while the feed ships counter dicts (`{total, by_status, open_count, open}`), so its stat tiles showed dict-key counts — fixed in the same PR (template + route + fixture now match the contracted shape, plus an Open-bugs list the dict shape provides for free). Enforcement is deliberately split: field-level checks live producer-side in superbot's CI (where the shape is constructed); the consumer runs the two cheap render-time checks that make drift visible where the owner looks.
- provenance: websites PR #11 (console-feed-contract session, 2026-07-09); superbot PR #1884 + superbot `.sessions/2026-07-09-kl6-console-telemetry.md` (the idea's origin); docs/dashboard.md § Data source

## [D-0011] Control-plane + dashboard made PUBLIC (Basic auth dropped); readiness secrets cell masked to a count
- status: superseded
- date: 2026-07-09
- superseded-by: D-0012
- verdict: The two formerly-private websites services are now **public** — the HTTP Basic auth gate is removed from both `control-plane` (`app/main.py`) and `dashboard` (`dashboard/app.py`). Every route serves without credentials; the `_authorized` helper + `auth_gate` middleware + now-dead `base64`/`secrets` imports are deleted, `/healthz` still works, and the apps no longer fail-closed 503 when `SITE_PASSWORD` is absent (they run fully public). `SITE_PASSWORD` is left set-but-unused in Railway (non-destructive, reversible); the dead `dashboard/config.py` (which only held it) is removed and `app/config.py` drops the var. The **one security refinement**: because the control-plane readiness board renders GitHub Actions **secret NAMES** (obtainable only with an admin-scope token — the single non-public datum on an otherwise public-repo-derived board), the secrets cell now shows a **COUNT only** (`N secret(s)`). The individual names are never rendered in the board HTML nor serialized into `/api/readiness.json` (the raw secrets API response is no longer carried in the returned row). Everything else on the board (rulesets, required checks, CODEOWNERS presence, auto-merge, open PRs, journal contents) is derived from public repos and is unchanged. Tests rewritten for the public model; a new assertion verifies a secret name (`ANTHROPIC_API_KEY`) is absent from the served board HTML and JSON. `botsite` was already public and is untouched. Deployed non-destructively to the `superbot-websites` project only (RAILWAY_API_KEY + explicit superbot-websites IDs; ambient production-bot IDs never passed; no destructive mutation).
- why: The owner directed it in-session, verbatim "Yes drop the auth" — the passwords were judged unnecessary friction, and the surfaces are read-only oversight of public data (superbot's committed JSON artifacts + public repo config), so there is nothing to gate. The single genuinely non-public datum, the Actions secret names, is masked to a count rather than dropped so the board keeps its "N secrets configured?" signal without exposing what they are. Owner-directed work merges immediately on green (Q-0191-class); the change is reversible (re-add the middleware / the var is still set) and covered by tests.
- provenance: websites PR #12 (drop-auth session, 2026-07-09); owner verbatim "Yes drop the auth"; docs/site.md § Auth, docs/dashboard.md, docs/deployment.md

## [D-0012] Password-gated `/owner` area on control-plane (public site unchanged; full detail + real actions)
- status: decided
- date: 2026-07-09
- supersedes: D-0011
- verdict: The control-plane main site stays 100% public and byte-identical (the D-0011 masking is preserved — the public board and `/api/readiness.json` still show an Actions-secrets COUNT only, and a test asserts zero secret names in the public HTML/JSON). A single gated area `/owner` is added (`app/owner.py`, an included `APIRouter(prefix="/owner")` — the public routes in `app/main.py` are untouched). The gate is HTTP Basic (any username), password compared constant-time (`secrets.compare_digest`) to `SITE_PASSWORD`: no/wrong creds → 401; correct → 200; `SITE_PASSWORD` UNSET → the `/owner*` routes fail closed with 503 while the public site keeps serving. `SITE_PASSWORD` (left set-but-unused on the service since D-0011) is read again, but ONLY by the owner router. `/owner` renders the readiness board UN-masked — the real Actions secret NAMES per repo, threaded exclusively through a new `readiness.board(reveal_secrets=True)` path that the public `board()` call never sets (so the public output cannot regress) — plus broken-check and oldest-PR detail; `/owner/api/readiness.json` is the authed JSON with names. Privileged actions (POST, same gate, reversible, using creds already on the box): `refresh` clears the in-memory TTL cache in-process; `rerun-ci` looks up the latest FAILED Actions run on a selected repo's default branch and POSTs `rerun-failed-jobs` via the existing `GITHUB_TOKEN`, degrading honestly on 403/no-failed-run. Added `python-multipart` (pinned) for the POST form. DELIBERATELY NOT WIRED, pending separate owner approval: any Railway account-token action and any live production-bot control API — no `RAILWAY_API_KEY` is present in the service env, and no bot control-API URL/token exists anywhere in the app.
- why: The owner directed it in-session, verbatim "we could add a password to a secure part of the site, one that gives you actual power to do something and view all secrets etc, but the main site should always be browsable." This is a targeted, reversible refinement of D-0011: D-0011 dropped auth from the whole site and masked secret names to a count; this keeps that public posture intact but adds ONE gated overlay for the detail + power the owner wants, rather than re-privatizing anything. A per-router gate (not global middleware) is the simplest solid option that guarantees the public surface stays credential-free and byte-identical. The two genuinely powerful-and-external levers (Railway account actions, live-bot control) are left unwired on purpose — they cross into irreversible/production territory that is its own owner decision, so the safe default is the read-detail + reversible-actions surface shipped here with zero production-control credentials present. Owner-directed → merges immediately on green; fully reversible (remove the router / the gate is additive).
- provenance: websites PR #14 (owner-area session, 2026-07-09); owner verbatim intent above; docs/site.md § "Public site + gated /owner area"

## [D-0013] Engage the substrate-kit machinery (render docs, CI gate, backfill, session loop)
- status: decided
- date: 2026-07-09
- verdict: The kit adoption (PR #1) was ritual-clean but never engaged; this pass turns it live. Rendered the 8 planted binding docs for real (filled architecture_layers, ownership_model, new_area_ownership, mutation_seam, drift_resolution, staleness_review, owner_profile, review_ritual from the project config via 'bootstrap render --live') and hand-filled their core tables so no UNRENDERED-SLOTS banner remains. Installed the kit CI gate .github/workflows/quality.yml (Python 3.12; 'bootstrap.py check --strict --require-session-log' + the three app pytest suites, on PRs and pushes to main). Backfilled bookkeeping: a retro .sessions/ card for PR #10 and PRs #4/#10/#13/#15 into current-state Recently-shipped. Routed the 7 rework-plan open questions into docs/question-router.md as Q-0001..Q-0007. Engaged the kit session loop: ran session-close (mined reflections + rebuilt .substrate/episodic_index.json over 12 logs) and reconciled session_count 0 -> 12 to match the indexed session logs. Seeded .session-journal.md with a real quick-reference. Vendored bootstrap.py is byte-identical to substrate-kit dist HEAD, so no engine bump.
- why: Adoption planted the docs and checkers but nothing ran them: the binding docs were template shells with slot banners, no CI enforced the kit check, the session counter sat at 0, and merged PRs had drifted from the ledger. Engaging the machinery is contained, reversible, and forward-only, and it makes the workflow self-enforcing (the CI gate + born-red card ritual) instead of exhortative.
- provenance: websites PR (engage-kit session, 2026-07-09); owner-directed kit-hygiene task
