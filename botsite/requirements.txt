# Bot-site service runtime deps — pinned. Its OWN requirements (Root Directory =
# botsite), separate from the control-plane's, so each Railway service installs only
# what it needs. This is the PUBLIC, secret-free surface: NO httpx-auth/OAuth/DB-write
# deps here beyond the anonymous raw-GitHub fetch. httpx is used only to GET the public
# site.json over raw.githubusercontent.com.
fastapi==0.139.0
uvicorn==0.51.0
jinja2==3.1.6
httpx==0.28.1
# Multipart form parsing for the /testing screenshot upload (ORDER 018 PR2) —
# the service's first added dependency since launch, deliberately deferred
# from PR1. httpx also carries the server-side Anthropic exit-review call
# (testing_ai.py) — still no auth/DB-write deps here; the API key is a
# runtime env read, never a package.
python-multipart==0.0.32
