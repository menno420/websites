# websites · status
updated: 2026-07-11T13:55:00Z
phase: OWNER-DIRECTED BUILD (this session, claimed here): the ANTHROPIC REVIEW SITE — owner verbatim "create a website specifically for anthropic to review the way we have been running our projects … a clear visual explanation of the problems and successes as well as the process and the way we managed to grow so quickly." Built as the FOURTH independent service `review/` (botsite/dashboard pattern: own Dockerfile/requirements/tests, FastAPI + Jinja2 on vendored ds/, server-rendered, no client framework): overview + /process (bus, landing path, gates, glossary — written for an outsider) + /growth (SVG charts of PR merges / session cards / test functions per UTC day from git-derived committed snapshot, table view, milestones) + /successes + /problems (honest, evidence-linked — gate leak #19, stranded push + rescue #59, silent routine fires, 5× cron error, time-bomb tests, ledger drift, walls) + /healthz /version /story.json. Data model: `review/gen_snapshot.py` bakes real numbers into committed `review/data/snapshot.json` (Root-Directory deploys ship only the service folder — runtime repo reads impossible by design); missing snapshot → honest banner, tested. quality.yml runs the new suite. PR **#132** open READY (branch claude/anthropic-review-site, main merged in at 77b5a34/ba92728 era); first CI run red on exactly the designed born-red hold (job log: "Designed hold — not a CI failure to investigate"), card flipped complete, awaiting green — NOT merged by this session per its instructions. No collision: verified zero open PRs + no backlog/status claim on this scope at session start.
health: green (branch head at write: full FOUR-service suite 277 passed — 249 baseline post-#130 + 28 review tests; bootstrap check --strict green under kit v1.11.0)
kit: v1.11.0 · check: green · engaged: yes
last-shipped: #130 — route-level clock freeze (the continuous chain's slice 27; sibling #129 kit v1.11.0). THIS session's deliverable in flight: PR #132 (review/ service). SELF-REVIEW POINTER: docs/retro/self-review-2026-07-11.md (ORDER 011, #118).
blockers: none
routine: armed · cron 0 */4 * * * · last-fired 2026-07-11T12:05Z (relayed #124; next ~16:00Z — the ~16:0xZ nudge rituals right after it). Healthcheck cron: the 12:17Z slot STILL UNDELIVERED at 13:28Z (~71 min; watching, never gating — carried from slice 27).
landing: pushed-unmerged claude/anthropic-review-site (PR #132 READY, quality pending after the card flip; any session may merge on green per the relay doctrine — the work is complete and self-contained). Chain branches: all-merged; five prune-candidate branches unchanged (claude/order008-wake-1205z-heartbeat, claude/harden-verify, claude/rework-dashboard, claude/wire-github-token-docs, manager/control-plant — owner-attention: agents get 403 on branch deletion).
deployed: ba92728 · verified 2026-07-11T13:44Z — ALL THREE deployed services /version == main HEAD at check time (curl, three 200s). The fourth service (review) has NO Railway service yet — creation is the new ⚑ ask below.
rung: order (owner-directed build — full session goal)
tooling: pr-capable
orders: acked=001-011 done=001-011
⚑ needs-owner: THREE asks — canonical list in docs/owner/OWNER-ACTIONS.md (the new review-service creation joins the standing two); click-level walkthrough + prune list mirrored in docs/retro/self-review-2026-07-11.md § 2 (ORDER 011).
  ⚑ OWNER-ACTION
  WHAT: Create the fourth Railway service so the new program-review site (built for Anthropic reviewers) goes live.
  WHERE: railway.app → project superbot-websites → New → Service → GitHub repo menno420/websites.
  HOW: set Root Directory = review (its own Dockerfile at review/Dockerfile is picked up automatically, exactly like botsite/dashboard); branch = main; no environment variables needed (read-only, network-free service). After first deploy: <service-url>/healthz should return {"status":"ok"}.
  WHY-IT-MATTERS: the review site — process, growth charts, successes, and an honest problems page for Anthropic reviewers — exists in the repo (PR #132) but has no URL until the service exists.
  UNBLOCKS: a shareable live URL for the Anthropic review; then the board's drift row + scripts/healthcheck.py can add the fourth service (agent-buildable the moment the URL exists).
  VERIFIED-NEEDED: service creation is a Railway account mutation — the Railway-safety policy (docs/RAILWAY-SAFETY.md) forbids agent-initiated Railway mutations without your explicit go; deliberately not attempted (same policy wall as the Postgres ask).
  ⚑ OWNER-ACTION
  WHAT: Create a small Postgres for the botsite /submit intake and point the botsite service at it.
  WHERE: railway.app → project superbot-websites → New → Database → PostgreSQL; then service botsite → Variables.
  HOW: add variable DATABASE_URL = the new Postgres connection string Railway shows (copy-paste).
  WHY-IT-MATTERS: the public feature/bug submission form is a labeled stub until a store exists.
  UNBLOCKS: the moderated submissions queue + GitHub-issue mirror (rework Q5) — agent-buildable the moment the variable exists.
  VERIFIED-NEEDED: provisioning creates a paid resource in your Railway account and D-0005 forbids agent-initiated Railway mutations without your explicit go — policy wall, deliberately not attempted; no DATABASE_URL exists on the service today.
  ⚑ OWNER-ACTION
  WHAT: Mint a durable fine-grained GitHub PAT and set it on the control-plane service.
  WHERE: github.com → Settings → Developer settings → Fine-grained tokens; then railway.app → superbot-websites → control-plane → Variables.
  HOW: token scoped to menno420 repos, read for contents/actions + actions:write for the CI re-run button; set as GITHUB_TOKEN (exact steps: docs/deployment.md § owner TODO).
  WHY-IT-MATTERS: rate headroom + resilience — the fleet surfaces walk 18 lanes tokenless on the anonymous 60-req/h ceiling; the board now also fetches 4 status files + the ideas path per cold load; headroom is the binding constraint.
  UNBLOCKS: actions-secrets + auto-merge-allowed cells; /owner re-run CI; 5000 req/h headroom across the fleet surfaces.
  VERIFIED-NEEDED: live board shows "unknown (token lacks admin scope)" / "unknown (needs push-scope token)" cells — the token is an owner-held Railway service variable agents cannot read or set. Live finding 2026-07-10/11: all fleet surfaces verified 200 with REAL content while the service token is unset.
notes: THIS SESSION CLAIMED + BUILT the anthropic-review-site (owner-directed; no inbox order id — announced here so the ~16:00Z wake and the manager don't re-route it; PR #132 is the artifact, merge-on-green welcome). Post-merge follow-ups for any session: (a) run `python3 review/gen_snapshot.py` when numbers should refresh (snapshot-aging idea captured in the backlog); (b) once the owner creates the Railway service, add its URL to scripts/healthcheck.py + the board drift row. Backlog: 5 buildable captured (order-ack latency line; nav-scan glob; inbox relay-order provenance advisory; cross-service clock pattern — dormant by design; NEW review-site snapshot-aging banner) plus the two manager-side asks. Standing flags carried from slice 27: (1) REVIEW-QUEUE ROWS OWED — websites #67, #72, #75, #77 (+#81 confirmed; #86→#130 borderline; #132 will owe one too — runtime lines well over threshold); (2) lanes.json ask; (3) meta.md convention ask. Rails held: forward-only, inbox untouched, one writer per file, trigger untouched, no model IDs in commits/PRs.
