# websites · status
updated: 2026-07-09T17:24:42Z
phase: Self-directed continuation — shipped `/activity.xml`, an Atom 1.0 feed over the cross-repo `/activity` timeline (subscribable fleet-activity feed; realizes the captured idea). Read the inbox FIRST (no new order — 001–004 unchanged), wrote status.md LAST.
health: green (all local gates green this session: `bootstrap.py check --strict --require-session-log` exit 0, full app suite 125 passed — 121→125 with +4 Atom tests, ambient-Railway-ID guard OK; control-plane auto-deploys to this PR's head on merge and was live-verified post-deploy — `/activity.xml` 200 + `application/atom+xml` + well-formed Atom with real PR entries, `/activity` Subscribe + discovery link, `/healthz` 200, all three services in-sync at the new head)
last-shipped: #41 — `/activity.xml` Atom feed over the cross-repo activity timeline ([D-0025]): a second serializer over the exact same TTL-cached `activity.timeline()` list (no new fetch/dependency/secret/Railway op), built with `xml.etree.ElementTree` so every field is XML-escaped; each dated PR → an `<entry>` (title, id=PR URL, updated=real merge/update time, link, author, summary); honest degradation (undated PR omitted, failed fetch still yields a valid feed with one diagnostic entry); `/activity` gains a `<link rel=alternate type=application/atom+xml>` discovery tag + a Subscribe (Atom) link.
blockers: none
orders: acked=001,002,003,004 done=001,002,003,004
⚑ needs-owner: (unchanged from PR #40; all pre-existing forks in `docs/owner/OWNER-ACTIONS.md`) 1) custom domains for the 3 sites (Railway → project superbot-websites → each service → Settings → Networking → Custom Domain → add + CNAME at DNS); 2) provision `/submit` submissions Postgres (Railway → project → New → Database → Postgres, then set DATABASE_URL on the botsite service) [Q5]; 3) wire dashboard `/admin` to a live-bot control API — prod writes, needs owner go, decide where bot control lives [Q4]; 4) preserve v1 visual design vs the shipped restyle (taste) [Q2]; 5) OLD-site cutover/retirement in superbot — go/no-go; 6) redeploy-from-browser scoped Railway deploy hook — yes/no; 7) optional: paste the production bot's public health URL if one exists (adds a liveness signal). DONE by owner: `quality` is a REQUIRED check on `main`.
notes: Self-directed continuation (not a new order). One route + one serializer + a discovery link — the whole feature rides the existing cached data source; the feed is a natural push surface complementing the pull-only `/activity` page now that ten Projects run in parallel. Follow-up idea captured (`docs/ideas/activity-per-repo-filter-2026-07-09.md`): an optional `?repo=` filter so a reader can subscribe to a single lane's feed. Rails held: forward-only, websites repo only, superbot/superbot-next/substrate-kit read-only, no secrets, no ambient RAILWAY_* IDs, no destructive ops, inbox.md never edited.
