# websites · status
updated: 2026-07-09 (session)
phase: ORDER 001 shipped and reconciled; dashboard flaky auto-deploy root-caused and fixed; all three services green and in-sync at main HEAD 5ed3bf66.
health: green (all three services in-sync at main HEAD 5ed3bf66 — control-plane, botsite, and dashboard all report a matching sha on /version, live-verified this session; the superbot-next report/golden-parity check is red-by-design as always, not broken)
last-shipped: #29 — deploy-trigger fix proof: recreated the dashboard's GitHub push→deploy trigger and merged a doc/test change to prove auto-deploy works; all three services auto-deployed 5ed3bf66 (no manual redeploy).
blockers: none
orders: acked=001 done=001
⚑ needs-owner: (parked, from docs/owner/OWNER-ACTIONS.md — none are agent-executable) Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go).
notes: RESOLVED — the flaky dashboard auto-deploy (previously a standing observation the owner might check eventually) is root-caused and fixed. Root cause: the dashboard Railway service was missing its GitHub push→deploy trigger (auto-deploy off) while control-plane and botsite each had one — that is why the dashboard repeatedly lagged behind merges and needed manual pinned-SHA redeploys. Fix: recreated the trigger (non-destructive, reversible; trigger id 6bc63516-b290-4f37-9b25-b155be077cde), then proven by PR #29 auto-deploying (deployment 89dfcb85, SUCCESS) with no manual redeploy — all three services now stay in sync automatically. The deploy-state drift cell (ORDER 001) is what originally surfaced this lag; it is working as intended. Rails held: forward-only, websites repo only, non-destructive/reversible trigger recreation (no RAILWAY_API_KEY/secret, no destructive Railway op).
