# websites · status
updated: 2026-07-09
phase: shipped ORDER 002 — a public `/fleet` heartbeat page on the control-plane board that renders every fleet lane's control/status*.md as one glanceable screen (which agents are running + how far along). PR #35 merged; control-plane auto-deploys; live-verified against the running deploy.
health: green (all three services in-sync at the new head — control-plane/botsite/dashboard deploy-state all `in_sync`, verified live via /api/readiness.json; scripts/healthcheck.py all 200; full app suite green, 111 passed; quality gate green on #35)
last-shipped: #35 — /fleet page ([D-0021], ORDER 002): renders all 10 fleet lanes (superbot, superbot-next, substrate-kit, websites, trading-strategy, codetool-lab-fable5/opus4.8/sonnet5, superbot-games ×2) from their control/status*.md — parsed heartbeat fields (updated-age + stale badge, phase, health with green/red/red-by-design indicator, last-shipped, blockers, ⚑ needs-owner), last-commit age + open-PR count per repo, full status body rendered as markdown, GitHub-deep-linked, attention-sorted, honest per-lane absence/error. Reuses the board's TTL-cached github layer + journal markdown renderer; +8 tests; no new dependency, no secret, no Railway op. Before that: #33 (/activity + /ideas), #31 (kit v1.0.0→v1.2.0 upgrade).
blockers: none
orders: acked=001,002 done=001,002
⚑ needs-owner: (1) /fleet lane set — the app holds a hand-kept `config.FLEET_LANES` copy of the manager's canonical registry `menno420/superbot` → `docs/eap/fleet-manifest.md`; keep it in sync as lanes are added, or approve evolving /fleet to parse the manifest live (D-0021 session idea). (2) Parked from docs/owner/OWNER-ACTIONS.md (none agent-executable): Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go).
notes: /fleet shipped and live — the owner now has one screen for the whole fleet's heartbeats. Live-verify evidence for #35: /fleet 200 renders the websites lane's own real status (health green, updated-age, orders acked=001,002, last-shipped #35) plus the other 9 lanes (real status where present — superbot-next red-by-design, substrate-kit/trading-strategy/codetool-lab ×3/superbot-games ×2 green — and the bare `superbot` lane as an honest "no status file" absence since its heartbeat is written to superbot-next); /fleet.json total=10; /healthz 200; scripts/healthcheck.py all 200; board deploy-state in-sync at the new head. Rails held: forward-only, websites repo only, control-plane service only, no secrets, no ambient RAILWAY_* IDs, no destructive ops, stubs untouched.
