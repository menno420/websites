# websites · status
updated: 2026-07-09T16:35Z
phase: shipped a self-directed improvement — the two live-monitoring surfaces (the readiness board `/` and `/fleet`) now AUTO-REFRESH client-side (soft in-place `#live-content` swap every 45s, visible indicator + pause/resume toggle) so the owner's control glance stays current without a manual reload; content/journal pages stay static. Also audited the required `quality` CI gate and CONFIRMED it runs the full suite on every normal PR (no change needed). PR #37 merged; control-plane auto-deploys; live-verified against the running deploy.
health: green (all three services in-sync at the new head — control-plane/botsite/dashboard deploy-state all `in_sync`, verified live via /api/readiness.json; scripts/healthcheck.py all 200; full app suite green, 121 passed; quality gate green on #37)
last-shipped: #37 — auto-refresh on /fleet + board `/` ([D-0023], self-directed): `app/static/autorefresh.js` (vanilla JS, no new dep) re-fetches the current page every `AUTOREFRESH_SECONDS` (45s) and swaps only the `#live-content` region in place — no reload flash, no duplicated render logic (server stays the one renderer), fetch omits `?refresh=1` so it rides the TTL cache. Visible "auto-refreshing every Ns · last updated <time>" indicator + pause/resume toggle (persisted; respects prefers-reduced-motion + tab visibility); interval is a config constant; new `StaticFiles` mount at `/static`. Scoped strictly to the two monitoring screens — content/journal pages stay static. +4 tests (117→121); no secret, no Railway op, superbot untouched. Before that: #36 (/fleet live from manifest), #35 (/fleet page, ORDER 002).
blockers: none
orders: acked=001,002 done=001,002
⚑ needs-owner: Parked from docs/owner/OWNER-ACTIONS.md (none agent-executable): Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go).
notes: This session was self-directed (not a new order); orders 001/002 remain done. quality.yml finding: the required `quality` gate runs the FULL suite on every normal PR (install all three services' deps + `bootstrap check --strict` + ambient-Railway-ID guard + `pytest tests/ botsite/tests dashboard/tests`; a recent run's log shows `117 passed in 3.61s` — the ~17s total is just fast, not a skip). The only short-circuit is the intentional, documented KL-8 `control/**`-only fast lane (heartbeat writes); no change made. Live-verify evidence for #37: served `/fleet` 200 + board `/` 200 both carry the auto-refresh indicator (`class="autorefresh"`, `data-autorefresh="45"`, `id="live-content"`) + the `/static/autorefresh.js` poll script; a content page (`/journal/websites`) carries none of that markup (static); `/fleet.json` + `/api/readiness.json` 200; `/healthz` 200; scripts/healthcheck.py all 200; board in-sync at the new head. Rails held: forward-only, websites repo only, control-plane service only, superbot read-only, no secrets, no ambient RAILWAY_* IDs, no destructive ops, stubs untouched.
