# websites · status
updated: 2026-07-09T17:05Z
phase: Ran the full owner-directed project self-review + fleet wake-up pass. Read the inbox FIRST; found ORDERS 003 (gen-1 retro) and 004 (Model-line backfill) were still genuinely unexecuted — PRs #38/#39 were the manager APPENDING those orders to inbox.md, not the Project executing them. Executed both, plus wrote the full project-review document. Status.md written LAST per the ritual.
health: green (all three services verified live this session — control-plane/botsite/dashboard `/healthz` 200 and `/version` all report sha `6abe19f` = current `main` HEAD, so all in-sync/no drift; `scripts/healthcheck.py` all 200; `bootstrap.py check --strict` green after the 18-card Model backfill; full app suite green on the last runtime PR, 121 passed)
last-shipped: #40 — project self-review + wake-up pass ([D-0024]): answered every gen-1 retro question by ID (`docs/retro/self-review-2026-07-09.md`, ORDER 003), backfilled the `📊 Model:` line on the 18 pre-kit-v1.2.0 `.sessions/` cards clearing the CI mtime-fallback landmine (ORDER 004), and wrote `docs/retro/project-review-2026-07-09.md` — true current state + full agent audit (every session/subagent, classified stalls, honest model attribution) + efficiency verdict + owner-actions + continuation. Docs-only; no `app/` change.
blockers: none
orders: acked=001,002,003,004 done=001,002,003,004
⚑ needs-owner: (mirrors project-review §(e); all pre-existing forks in `docs/owner/OWNER-ACTIONS.md`) 1) custom domains for the 3 sites (Railway → project superbot-websites → each service → Settings → Networking → Custom Domain → add + CNAME at DNS); 2) provision `/submit` submissions Postgres (Railway → project → New → Database → Postgres, then set DATABASE_URL on the botsite service) [Q5]; 3) wire dashboard `/admin` to a live-bot control API — prod writes, needs owner go, decide where bot control lives [Q4]; 4) preserve v1 visual design vs the shipped restyle (taste) [Q2]; 5) OLD-site cutover/retirement in superbot — go/no-go; 6) redeploy-from-browser scoped Railway deploy hook — yes/no; 7) optional: paste the production bot's public health URL if one exists (adds a liveness signal). DONE by owner: `quality` is a REQUIRED check on `main`.
notes: This was a full self-review pass, not just a heartbeat. Key findings for gen-2 setup (full detail in the retro + project-review docs): (i) the single clearest platform gap across all sessions was NO scheduler/`send_later` primitive — "hourly monitoring" could never be armed as a timer; (ii) early parallel workers sharing one git checkout cost superseded PRs #5/#9 — self-fixed by per-worker fresh clones + serialized ledger writes (recommend as a founding rule); (iii) the dashboard Railway service shipped with no push→deploy trigger (fixed PR #29) — create the trigger at service-creation time; (iv) gate-tightening kit upgrades should ship a grandfather/backfill step (the v1.2.0 Model-line requirement left the ORDER 004 landmine). Verified merged set: 37 PRs (#1–#39 minus #5/#9/#22 superseded); decision ledger D-0001..D-0024; 26 session cards (all now carry the Model line). Rails held: forward-only, websites repo only, superbot/superbot-next/substrate-kit read-only, no secrets, no ambient RAILWAY_* IDs, no destructive ops, inbox.md never edited.
