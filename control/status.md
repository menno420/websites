# websites · status
updated: 2026-07-09T16:30Z
phase: shipped a self-directed improvement to the `/fleet` control-plane page — the fleet lane SET is now derived LIVE from the manager's canonical manifest (`menno420/superbot` → `docs/eap/fleet-manifest.md`) instead of a hand-kept `config.FLEET_LANES` copy, with an honest labeled fallback; and fixed this row's own timestamp to full ISO so `/fleet` shows a real freshness age. PR #36 merged; control-plane auto-deploys; live-verified against the running deploy.
health: green (all three services in-sync at the new head — control-plane/botsite/dashboard deploy-state all `in_sync`, verified live via /api/readiness.json; scripts/healthcheck.py all 200; full app suite green, 117 passed; quality gate green on #36)
last-shipped: #36 — /fleet lane set live from the fleet-manifest ([D-0022], self-directed): `fleet.resolve_lanes` fetches the manager's manifest (superbot READ-ONLY, same TTL cache), `parse_manifest` reads its markdown table by header name, `manifest_to_lanes` expands rows into lanes (manager row skipped; multi-repo SuperBot coordinator → superbot + superbot-next; shared superbot-games repo → status-<slug>.md). Verified live: yields exactly the 10 lanes, identical (repo, status_path) to the old list — a lane added to the manifest now auto-appears (drift removed). Manifest fetch/parse failure falls back to `config.FLEET_LANES` with a visible "cached fallback list" notice (/fleet.json `lane_source`=manifest|fallback); an unreadable-repo lane renders honestly, not dropped. +6 tests (111→117); no new dependency, no secret, no Railway op. Before that: #35 (/fleet page, ORDER 002), #33 (/activity + /ideas).
blockers: none
orders: acked=001,002 done=001,002
⚑ needs-owner: Parked from docs/owner/OWNER-ACTIONS.md (none agent-executable): Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go). RESOLVED this session: the `/fleet` lane-set "hand-kept `config.FLEET_LANES` / keep in sync or parse live" drift flag (D-0021) — `/fleet` now parses the manifest live (D-0022), so no owner sync action is needed.
notes: `/fleet` is now self-updating against the manager's manifest — adding a lane there makes it appear on the board with no websites code change. This session was self-directed (not a new order); orders 001/002 remain done. Live-verify evidence for #36: `/fleet` 200 renders "live from manifest" (10 lanes parsed, not the fallback notice); `/fleet.json` `lane_source.source=manifest`, summary.total=10; the websites row shows a real updated-age from this full-ISO timestamp (not "age unknown"); /healthz 200; scripts/healthcheck.py all 200; board deploy-state in-sync at the new head. Rails held: forward-only, websites repo only, control-plane service only, superbot read-only (fetched, never written), no secrets, no ambient RAILWAY_* IDs, no destructive ops, stubs untouched.
