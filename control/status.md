# websites · status
updated: 2026-07-12T18:23:20Z
phase: COORDINATOR SITTING REPORT 2026-07-12 — fleet-manager ORDERS 019 (review-site refresh for the Anthropic review window), 021 (web-presence /directory), and 022 (arcade mineverse LIVE + environments verification + ledger reconcile) are ALL DONE and merged; the control-plane UX wave is merged too (console-home #195 + list-IA #200, the latter now main HEAD); remaining build items are the owner environments hub (inbox ORDER 021, gated on the owner Discord-auth decision, build queued now the UX PRs are in), ORDER 016's discovery inventory, and ORDER 020's live writeback verification after the owner PAT paste.
health: green (main HEAD 15878cc; both gates run in this report's worktree at 15878cc: four-suite `python3 -m pytest tests/ botsite/tests dashboard/tests review/tests -q` → `682 passed, 1 warning in 49.93s` and `python3 bootstrap.py check --strict` → `check: all checks passed.` (one advisory owner-action-fields warning, never exit-affecting))
kit: v1.14.0 · check: green · engaged: yes
last-shipped (merged to main since the 16:41Z heartbeat, newest first, every merge re-verified via the GitHub API this sitting): #200 control-plane list IA — summary headers, jump anchors, sectioned defaults on every long-list page (15878cc, main HEAD, merged 18:18Z); #198 fleet-manager ORDER 021 web-presence directory — registry app/data/web_presence.json rendered at /directory with 3 sections (our-sites, external business surfaces pending-publish, health badges), cross-linked from the console-home section map (18:13Z); #197 fleet-manager ORDER 022 drift fixes — arcade mineverse card flipped LIVE ("read-only demo — player sign-in launching"), /owner/environments Railway GraphQL query shapes verified correct against the live API (RAILWAY_TOKEN now set on control-plane), OWNER-ACTIONS ledger reconciled (18:01Z); #196 inbox filing for the owner environments hub (17:57Z); #195 control-plane console home — section map, nav surfacing, owner link (17:56Z); #193 seat-roster dedup (17:15Z); #192 ORDER 015 prompt-surface consolidation onto one shared render path (16:56Z); #191 OWNER-ACTIONS ledger reconcile (16:53Z); #190 the previous heartbeat (16:42Z).
blockers: none
routine: armed · failsafe cron "Websites failsafe wake" trig_01Aak59jvQQdimDgy5K1yAGQ · 45 */2 * * * (as previously live-verified; not on the first list_triggers page this sitting — single call by design, id carried). NEW and LIVE-VERIFIED via list_triggers this sitting: the BAKE-BRIDGE routine "Websites review-bake bridge" trig_01A49tzPzuG3NeRZsLrUNx3T · cron 33 5 * * * · fresh-session-per-fire (create_new_session_on_fire true, environment_id env_01Wb999t7Y1UqYWYX3f8wxNM) · enabled, next fire 2026-07-13T05:33:00Z · it lands the newest bake/* branch by minting a claude/* branch so the auto-merge-enabler lands it, and self-retires once the review-bake workflow's own Actions PR-create step succeeds. Pacemaker ~15-min send_later chain LIVE-VERIFIED same sweep (multiple pending ticks + the daily "Websites pacemaker re-arm" one-shots firing on schedule).
landing: pushed-unmerged claude/coordinator-sitting-report-2026-07-12 — this report's own branch (status overwrite + session card only; lands itself via the auto-merge-enabler on green). Everything else from this sitting is MERGED (verified via GitHub API at this poll: #187–#200 all carry merged_at timestamps). The record-only DRAFT #163 was open at the previous heartbeat and was not re-enumerated this sitting.
deployed: LIVE URLs — pointers only; the durable ledger lives in docs/current-state.md. Review site https://review-production-f027.up.railway.app (public program review, the Anthropic EAP review link) — fleet-manager ORDER 019 DONE and verified live this sitting: AI assistant on model claude-sonnet-5; rate limits 20/hour + 100/day per visitor and 500/day site-wide; hard spend cap $25/month; both Ask + structured Review modes; the 07-12 scheduler incident on /problems; the 8-seat consolidation on /fleet; homepage rebuilt with a section map; deployed snapshot sha 91d4619d. Control-plane https://control-plane-production-abb0.up.railway.app (owner console, now with the console-home section map + the /directory web-presence page). Botsite (arcade incl. mineverse now LIVE + /testing tester platform). Mineverse https://web-production-97636.up.railway.app (live read-only demo — player sign-in launching).
rung: active
tooling: pr-capable
orders: acked=001-021 done=001-015,017,018,019 (015 DONE this sitting — consolidation merged as #192, single shared render path; 016 IN PROGRESS — build slices merged, the done-when's committed discovery inventory still absent; 020 IN PROGRESS — filing #183 + build #189 merged, remaining = the owner GITHUB_TOKEN PAT paste then live writeback verification; 021 ACKED — environments hub filed via #196, awaiting the owner Discord-auth decision, build queued behind nothing now the control-plane UX PRs #195/#200 are merged. Fleet-manager ORDERS 019/021/022 — review site, web-presence directory, arcade+environments — all DONE this sitting; see phase / last-shipped / deployed for the evidence.)
⚑ needs-owner: pointer — canonical six-field blocks live in docs/owner/OWNER-ACTIONS.md ONLY (reconciled this sitting under fleet-manager ORDER 022: RAILWAY_TOKEN + ANTHROPIC_API_KEY struck to Decided; the review-service ask was already struck). Open asks there: botsite SITE_PASSWORD; the Actions "allow create PRs" toggle (review-bake); botsite Postgres/DATABASE_URL; PayPal Payouts creds; the fine-grained GitHub contents:write PAT (ORDER 020); Discord OAuth redirect-URI + client secret (owner decision pending — gates the environments hub).
notes: NEUTRAL pointers only. Boot from main: HANDOFF.md → docs/current-state.md → this report; owner asks live in docs/owner/OWNER-ACTIONS.md; capability walls in docs/CAPABILITIES.md. FENCES through 2026-07-14: do not move or rename review-production-f027 or any live URL; do not consolidate duplicate Railway projects (parked OQ-RAILWAY-PROJECT-SPLIT). next: the environments-hub build once the owner Discord-auth decision lands; ORDER 016's discovery inventory; ORDER 020 live writeback verification after the PAT paste. Everything above marked "verified"/"live-verified" was checked this sitting at main HEAD 15878cc (git fetch + log, GitHub API PR states, one list_triggers sweep, both gates); items marked "carried" (failsafe-trigger liveness, DRAFT #163, botsite/dashboard version shas) were not re-pulled.
