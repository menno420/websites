# websites · status
updated: 2026-07-09T15:25Z
phase: shipped two new public control-plane views — /activity (cross-repo PR timeline across all four fleet repos) + /ideas (docs/ideas/ backlog per repo), on the board's existing TTL-cached github layer. PR #33 merged, control-plane auto-deployed to da35e21f, live-verified.
health: green (all three services in-sync at the new head da35e21f — control-plane/botsite/dashboard deploy-state all `in_sync`, verified live via /api/readiness.json; scripts/healthcheck.py all 200; full app suite green, 103 passed; quality gate green on #33)
last-shipped: #33 — cross-repo /activity timeline + /ideas backlog views ([D-0020]). /activity merges recent PRs across superbot/superbot-next/substrate-kit/websites into one reverse-chron, repo/state-badged, GitHub-deep-linked stream with honest per-repo error banners; /ideas surfaces the docs/ideas/ backlog per repo (title + one-line parsed, newest enriched + rest counted, no-ideas repo = honest absence, each idea deep-linking to GitHub + the in-app markdown view). Nav links added; +9 tests; no new dependency, no secret, no Railway op. Before that: #31 kit v1.0.0→v1.2.0 upgrade + engagement close-out.
blockers: none
orders: acked=001 done=001
⚑ needs-owner: (parked, from docs/owner/OWNER-ACTIONS.md — none are agent-executable) Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go).
notes: ORDER 002 (P1, status `new` in inbox — build a /fleet page rendering every lane's control/status*.md) is a NEW order beyond ORDER 001 and was NOT this session's scope; reported to the orchestrator for dispatch, not executed here (inbox.md never edited — manager owns it). Heads-up: PR #32's commit title reads "ORDER 002 — build /fleet page" but its diff only appended the order to inbox.md; /fleet is not built yet. Live-verify evidence for #33: /activity renders real cross-repo PRs (websites #33 merged, superbot-next #78 open, superbot #1897 open, substrate-kit #41 open, no fetch errors); /ideas lists real titles (superbot 221, substrate-kit 15, websites 1, superbot-next 0 = honest absence, only a README); /healthz 200. Rails held: forward-only, websites repo only, no secrets, no ambient RAILWAY_* IDs, no destructive ops.
