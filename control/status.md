# websites · status
updated: 2026-07-09T14:21Z
phase: substrate-kit upgraded v1.0.0 → v1.2.0 (sha256-verified §4.3 upgrade; PR #31); engagement gate reads ENGAGED; control fast lane now live in the required `quality` gate.
health: green (all three services in-sync at main HEAD per the board's deploy-state cell as of last check; kit `check --strict` green post-upgrade; full app test suite green locally)
last-shipped: #31 — kit v1.2.0 upgrade + engagement audit: vendored dist replaced (archive-first, rollback banked in .substrate/backup/), upgrade-report classes 15 consumer-edited / 3 diverged (the hand-planted control/ trio, pre-hash-record — kept as-is), .claude/CLAUDE.md + settings.json planted rendered via `adopt --include-claude`, remaining interview slots (integration_mode=guided, doc_roots=docs) answered, v1.2.0 control fast lane folded into quality.yml (control-only diffs short-circuit green in-job — heartbeats like this one no longer pay the heavy suite or need a session card).
blockers: none
orders: acked=001 done=001
⚑ needs-owner: (parked, from docs/owner/OWNER-ACTIONS.md — none are agent-executable) Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go). Standing observation (not blocking): the dashboard Railway service auto-deploy has been flaky before — worth watching after this merge.
notes: control/README.md and control/inbox.md classified `diverged` in the upgrade report (planted here from the superbot fleet spec before the kit shipped its own control/ templates, so no recorded hash). Kept the live copies — content matches the spec; the kit's generalized template additionally documents the CI fast lane + zero-check-runs lessons. Manager may re-plant README from the kit template at will; this project did not edit manager-owned files. Rails held: forward-only, websites repo only, no secrets, no ambient RAILWAY_* IDs.
