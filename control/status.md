# websites · status
updated: 2026-07-09 (session)
phase: ORDER 001 shipped and reconciled; all three services green and in-sync at main HEAD 791380c2; drift cell live and working as intended.
health: green (all three services in-sync at main HEAD 791380c2 — control-plane, botsite, and dashboard all read `in-sync 791380c2` on the board's deploy-state cell; the superbot-next report/golden-parity check is red-by-design as always, not broken)
last-shipped: #26/#27 — ORDER 001: deploy-state drift cell + /version endpoints (squash-merged to main); dashboard was redeployed to 791380c2 via a non-destructive pinned-SHA deploy to clear the auto-deploy lag, so it is now in sync with the other two services.
blockers: none
orders: acked=001 done=001
⚑ needs-owner: (parked, from docs/owner/OWNER-ACTIONS.md — none are agent-executable) Q4 dashboard /admin live-bot control (build/wire vs keep stub); Q5 botsite /submit submissions Postgres + moderation mirror (provision vs keep stub); redeploy-from-browser scoped Railway deploy hook (yes/no); custom domains (deferred to cutover); preserve v1 visual design vs shipped restyle; OLD-site cutover/retirement in superbot (go/no-go). Standing observation (not blocking): the dashboard Railway service has a flaky auto-deploy — it again lagged behind the merge and needed a manual pinned-SHA redeploy to catch up; low priority, but the owner may eventually want to check that service's deploy webhook/watch config.
notes: The deploy-state drift cell (ORDER 001) is exactly what surfaced the dashboard lag — working as intended. Lag is now cleared: all three services in-sync at 791380c2. Rails held: forward-only, websites repo only, non-destructive pinned-SHA redeploy (no RAILWAY_API_KEY/secret, no ambient production RAILWAY_* IDs, no destructive Railway op).
