# Claim — arcade link-bearing availabilities single source of truth

- `claude/arcade-link-truth-0713` · **backlog promotion: single source of truth for link-bearing arcade availabilities** — move the duplicated `("live", "download")` tuple to one `arcade.LINKED_AVAILABILITIES` constant consumed by `has_link` and the drift probe, plus a coverage pin test · botsite/arcade.py + botsite/arcade_probe.py + botsite/tests + the backlog bullet · 2026-07-13
