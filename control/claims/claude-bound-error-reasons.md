# Claim — bound error reasons at the envelope source

- `claude/bound-error-reasons` · **bound error reasons** — sanitize/truncate envelope `error` text in `github._result` so /fleet banners, owner UI and all consumers inherit the 140-char markup-free bound (PR #237 follow-up) · `app/github.py`, `tests/` · 2026-07-13
