# Claim — hostile-env poison-list self-deriving pin

- `claude/env-poison-pin-0713` · **backlog promotion: self-deriving poison
  list** — AST source sweep pinning `tests/test_hostile_env_smoke.py`'s
  `ENV_VARS` to the env-var names the four services actually read
  (`docs/ideas/backlog.md` bullet, captured 2026-07-13 from PR #287's 💡) ·
  tests/** + docs/ideas/** + .sessions/** only, no service code · 2026-07-13
