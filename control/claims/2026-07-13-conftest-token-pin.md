# Claim — ORDER 027 item 5 suite-level token pin

- `claude/conftest-token-pin-0713` · **ORDER 027 item 5 — suite-level token pin in `tests/conftest.py`** — autouse sentinel pinning `GITHUB_TOKEN`/`RAILWAY_TOKEN` for every control-plane test, killing the unpinned-reason-assertion flake class · tests/conftest.py + docs/ideas/backlog.md + .sessions/** only, no production code · 2026-07-13
