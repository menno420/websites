# Claim — committed-inventory consistency pin

- `claude/inventory-consistency-pin` · **committed-inventory consistency pin** — reconcile railway.SERVICES vs the envhub registry drift + one zero-network pin test · `app/railway.py`, `tests/`, `.sessions/` · 2026-07-13
