# Claim — env-guard static gate

- `claude/env-guard-gate-0713` · **env-guard static gate test** — one repo-wide test that fails CI on module-level bare `int()`/`float()` over env vars (backlog promotion from the PR #282 env-hardening session) · tests/** + docs/ideas/backlog.md + .sessions/** only · 2026-07-13
