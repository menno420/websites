# claim — wire BAKE_PAT into review-bake landing step

- `claude/wire-bake-pat` · **review-bake.yml BAKE_PAT wiring (ASK-0008)** — flip the landing step's `GH_TOKEN` to prefer the owner-added `BAKE_PAT` secret (falling back to `GITHUB_TOKEN`) · `.github/workflows/review-bake.yml` · 2026-07-18
