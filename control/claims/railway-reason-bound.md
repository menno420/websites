# Claim — railway-reason-bound

- `claude/railway-reason-bound` · **app/railway.py error-reason bounding** — route all Railway GraphQL error-reason minting through `short_reason()` (+ possible `app/github.py` export) · `app/railway.py`, `app/github.py`, `tests/` · 2026-07-13
