# Claim — scheduled Playwright smoke-crawl in CI

- `claude/smoke-crawl-0714` · **Scheduled browser-level smoke-crawl in CI (backlog promotion, #311 session's 💡)** — `scripts/smoke_crawl.py` + `.github/workflows/smoke-crawl.yml`: headless-Chromium crawl of the three public sites + the control-plane root every 6h, console/link/status assertions · scripts/** + .github/workflows/** + docs/ideas/backlog.md + .sessions/** · 2026-07-14
